# R-26 复测结论（pipeline.tester · 设备真机 · 2026-09-23）

对象：apk/测试版/hermes-pocket-R26-本地存储-20260923.apk（5062672 字节）
整包 sha256 = 407a23270b93bafc087412990bb04bac5ffda8dee8fe97fac0588cb2570d231e（与派活单一致）
stamp-build.py --verify = **PASS 包内↔源树**（本包与源树一致，与 R31 那次 FAIL 不同）
包内 build-info：testVersion=unified-20260923-082501 / builtAt=2026-09-23 08:25 CST / featureId=R-26
包内 talk.js sha256 = d9a7c8ff6c7bb5160ee598859310743ed65c43c5ab8428d836d8e5200cd90333（一致）
包内 cache.js sha256 = 182045b4fc4a4ea4b531e6581f32785476ccdb276292e5266a0a200c01173241（一致）
设备：emulator-5554 / Android 14 / AVD hpk34（容器 hp-emu）· adb install -r Success
**本次只测板块：设置页(存储) + 聊天页-群聊回归**（按 SPEC §10 一板块一驱动一证据）

## 判据（8 条）
① 容量上限 200→50 —— **通过**：走产品路径（paintChat → rpcCache → CACHE.set(thread.*)）喂 200 条，落盘 50 条（cacheMaxItems=50）；
   注意：视图仍渲染 200 条气泡，只裁缓存（不是界面裁）。
② 落盘节流 —— **通过**：draft.<角色>（throttle 500ms）连写 10 次 → Storage.setItem 真调用 **1 次**、HP.Cache.writes +1、合并 9 次、落盘值 = 最后一次（"v9"）。
③ 清理 2 键 —— **通过**：清前 18.4KB/8 键 → 清后 12.0KB/6 键；被删两键字节和 6.4KB = 占用下降 6.4KB（对账一致）。
④ 满配额 —— **通过**：让 setItem 抛 QuotaExceededError → set 返回 false、**0 条未捕获异常**、提示 **1 次**、degraded=true、
   lastError="配额满了：…"；降级后再写被跳过（skipped +1），不再反复撞墙。
⑤ 设置页读回占用 + 原生侧「暂不可用」—— **通过 + 如实显示**：读回行「本地占用：8 个键 · 21KB」；
   原生侧行「原生侧：暂不可用（等原生加只读口 app.storage）」（未做就是未做，不当通过也不当失败）；
   真触摸点「看明细」→ 卡里真多出各键明细（thread.pipeline.tester 7.8KB · thread.probe 7.8KB …）= 真触发。
⑥ 真机配额与 spill 实况（renderer 没测到的部分）—— 读数：逐块写直到抛错，**5178519 字符（约 5.06 MB / 5057 KB）才抛 QuotaExceededError**；
   `HP.Cache.spill` 不存在 → **本版没有 spill 分支**（满配额只有"降级不落盘"这一条护栏）。如实记录，不是通过也不是失败。
⑦ 无回归（本板块上次改动）—— R-29 群聊钉底：**5/5 true**（含此前那条红：等高/变矮/单条变矮追加后都 gap=0px、pinned=true、无入口）
   → 这版已含 R-29 修复；R-31 发送状态：**9/9 true**。
⑧ 异常 —— 本块自身 **0 条新异常**；页面全程录到 **2 条既有缺陷异常** `this.pullRoleOutput is not a function`（talk.js:1027/155，
   4 处调用 0 处定义）—— 不在本轮范围，如实记录。

## 坐标 / 键位 / 布局（SPEC §10；1 CSS px = 1 dp，dpr 2.75 → 物理 px = css × 2.75）
设置页(存储) 卡（滚进可视区后量，量完同一拍点）：
| 控件 | px(x,y,w×h) | 在可视区 | 命中自己 | 被顶栏/键条/输入条遮挡 | 数字截断 | 高≥44dp |
|---|---|---|---|---|---|---|
| 本地占用行 | 25,251,344×17 | 是 | 是 | 否 | 否 | 否(17) |
| 原生侧行 | 25,274,344×17 | 是 | 是 | 否 | 否 | 否(17) |
| 看明细键 | 25,300,71×42 | 是 | 是 | 否 | 否 | 否(42) |
| 清理键 | 104,300,57×42 | 是 | 是 | 否 | 否 | 否(42) |
卡内相邻行间距：6 / 8 / 0 px；**4/4 控件命中自己、0 遮挡、0 截断**。

聊天页-群聊（3 条消息）：
| 控件 | px(x,y,w×h) | 在可视区 | 命中自己 | 遮挡 | 高≥44dp |
|---|---|---|---|---|---|
| 抬头 | 14,109,365×17 | 是 | 是 | 否 | 否 |
| 消息区 | 12,132,369×240 | 是 | 是 | 否 | 是 |
| 喊话输入框 | 12,372,233×40 | 是 | 是 | 否 | 否(40) |
| 广播键 | 253,372,56×40 | 是 | 是 | 否 | 否(40) |
| 身份键 | 317,372,64×40 | 是 | 是 | 否 | 否(40) |
| 投递台账键 | 12,413,77×32 | 是 | 是 | 否 | 否(32) |
键位：**常驻按键 3 个（广播 / 本人说 / 投递台账）= 3 ≤ 3 ✓**；真触摸点「身份键」→ 文案 本人说 → **经理说**（asWho=owner.me）= 真触发 ✓；
6/6 控件命中自己、0 遮挡。误触高发点：台账键 32px 高、与消息区底部仅 8px 间隔（同屏相邻），点它附近容易落到消息区。
布局：气泡 3 条各 72px 高 × 182px 宽，内部 3 行（16/21/12 px = 名字行 / 正文行 / 时间行）、无横向溢出；空态文案「（还没有消息）」；
消息区滚到底 gap=0px（不可滚时不显示入口）。

截图：截图-设置页存储-20260923.png · 截图-聊天页群聊-20260923.png
原始读数：运行A-存储-原始输出.txt · 坐标键位布局-原始输出.json · 回归-R29钉底-原始输出.txt · 回归-R31发送-原始输出.txt
