一下优化和补充按照一下要求
**
架构规划和图形渲染优先于功能扩展，测试应该是针对修改部分修改
按照模块修改，按照模块打包，按照模块测试
**

一下是优化点，你需要先将这个目标写下来，按功能写下来。然后按模块写

1. 对于长文本，可能无法向下移动文本，往下拉。也可以一键往下拉，可以限制缓存存储文本长度。
2. 应该具有测试连通性，丢包率等等功能
3. 可以下载skill到本地并下载，可以修改
4. 可以知道系统提示词等等的信息
5. 可以修改模型
6. 可以知道session和在线状态，可以选择对于session会话，当然要选择指令，都在一个tmux中，且保持运行中的session还在运行session。那么从这里要求，启动该软件，如果有某个tmux就不管且选择这个tmux，如果没有tmux就创建tmux，但是始终不终止tmux。
7. 将多个功能应该是隐藏在左侧隐藏栏内，点击“三”就可以显示左侧隐藏栏，然后可以选择栏目
8. 统计历史的上行下行，可以参看cdp项目。并复制？
9. 好像调节字体之类的无效，我需要有效但是默认保持就是当前界面的状态
10. 连接总是说新建立连接，是每次都会上传key还是什么原因导致的这个问题
11. 不知道为什么加载完skill之后还需要加载很久的时间才加载聊天界面

12. 该软件完全可以监听聊天界面的输出，识别是否结束，是否在压缩，是否在运行等等状态
