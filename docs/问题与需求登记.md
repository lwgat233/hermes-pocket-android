# 问题与需求登记（Hermes Pocket · 第二轮）

格式：**编号 / 现象（用户原话式） / 根因 / 修法 / 证据 / 状态**。状态只有三个：待做 / 在做 / 已修（附证据路径）。
用户报的每一条都进这里；做完不许删除，只改状态并补证据路径。

| 编号 | 现象（用户原话式） | 根因 | 修法 | 证据 | 状态 |
|---|---|---|---|---|---|
| R-01 | 长文本没法往下拉；要能一键往下拉；要能限制缓存文本长度 | **已定位**：① 普通缓冲其实能滚（实测 300 行：下滑翻开 17 行、出现「⇣回到底部」、点它回到最新、入口消失）；"拉不动"主要在 **TUI（备用屏）**——本地没有历史，滚轮必须发给远端，而 tmux 默认没开鼠标；而且**滚上去之后没有回程的入口**（旧逻辑在备用屏里按 `baseY-viewportY` 算，实测恒为 1，显示成没用的「上面还有 1 行」）。② 「缓存上限」改了不生效 = R-09 的同一根因（两份状态 + 启动顺序） | ① 备用屏按"往外发了几格滚轮"给「⇣ 回到最新（TUI 里已往上翻 N 格）」入口，点它**反向发滚轮**把视图还回去；进出 TUI 时刷新入口文案（不再显示 `上面还有 N 行`）；② 长按终端菜单加一项「⇣ 回到底部 / 最新」（常驻按键不增）；③ 远端没开鼠标时仍然一次会话只问一次，不假滚 | `evidence/渲染与设置/F-UI-1-长文本滚动-原始输出.txt`（12 条断言全 true：普通 4 + 备用屏 8，另一路"没鼠标"验证弹出了「开启 tmux 鼠标 / 取消」）；`HP.BUILD=uidx-20260921-B`，APK sha256 `a6a77c56…f856687f` | 已修（本地测试台；**模拟器真机复验待做**） |
| R-02 | 要有连通性测试、丢包率 | 功能不存在（只有终端里手打 ping） | 新栏目「网络」（诊断集合，登记表生成）：① `net.ping` 远端跑 `LC_ALL=C ping -c N -W 2`（全丢包也用 `\|\| true` 保住输出，不让它变成一个没内容的异常）；② `net.tcp` 远端 python 建一次连接量耗时；③ 解析全在界面侧 `net.js`（纯函数）——**包数 / 丢包率 / 最小-平均-最大 RTT / 通不通 / 耗时 ms**；④ 行上一个测试项，点行就跑、跑完把数字写回行里；⑤ 要看原文点「原始输出」行（ping 与端口两段 + 远端耗时） | 2026-09-21 net-A | **已修**｜`t-net.mjs` 18/18（解析喂的是 `tools/check_net_py.py` 把 `Bridge.kt` 里命令**原样抽出来跑**抓的真实输出：回环 0% 丢包 / 100% 丢包 / 域名解析失败 / 端口通 / 端口被拒；含界面流程与 F-NET-8 统计纯函数）+ 10 个驱动回归，共 178 条断言全 true。APK sha256 `cbd9c0f1…d7bc2dd`。样本文件 `evidence/网络与流量/fixtures/net-samples.json` |
| R-03 | 能把 skill 下载到本地、能修改 | 原来只能读 + 在终端 `less` 打开（`panels.js:376 openSkill`），没有下载/回存；手机上要"改"只能整篇重打 | 技能行改由统一渲染器 `HP.UI.sheet` 打开（M1 的单一渲染入口）：① 只读 op `skill.read` 取**原文**（回带 sha256 + 字节数 + base64 正文，字节可对账）；② 〔下载到手机〕走新原生 op `local.save`——MediaStore 落 `Download/hermes-skills/`（Android 10+ 免权限；失败退 App 外部目录并如实报真实路径），**写完读回算 sha256**，与远端 sha 一致才在提示里说「与远端一致」；③ 〔编辑并回写〕走 `skill.write`：**带打开那一版的 sha256 对账**（远端被别人改过就拒绝，不静默覆盖）→ 改前备份 → 原子替换 → 读回算 sha256/字节数 | 2026-09-21 md-C | **已修**｜`t-skill.mjs` 23/23（sha256 全是真算：页面 WebCrypto + 驱动 node crypto）+ `tools/check_skill_py.py`（把 `Bridge.kt` 里两段远端 python 原样抽出跑：读 sha/size/正文一致；写→对账/备份/读回；**旧 sha 再写必须被拒**；共 167 条断言全 true）。APK sha256 `74db1ffc…c4781ab`。**手机侧 MediaStore 写入只能真机验**，测试台验的是名字/内容/sha 对账与失败如实报错 |
| R-04 | 能知道系统提示词等信息 | Hermes 页原来只有技能 + 记忆；系统提示词在远端是**代码组装**的，App 里没有任何来源；硬拼一份"像的"出来就是假证 | 新增只读 op `hermes.prompt`：远端用 python3 只读打开 `state.db`，从 `system_prompts(hash→prompt)` 取「最近会话用过的那一份」（`sessions.system_prompt_hash` 关联），回带标记的原始文本；界面侧 `assets/ui/remote.js` 解析成结构化数据，Hermes 栏目加一行「系统提示词」（次行写份数与长度、点行开小窗看正文、可看更多/复制）；取不到就如实说原因 | `evidence/Hermes远端/F-HERM-4-系统提示词-原始输出.txt`（11 条断言全 true：解析真实输出 / 坏数据如实报错 / 行渲染合规 / 抬头写清范围 / 来源写清在 state.db / 看更多真取下一段 / 到底了说已到底 / 取不到不装样子；另含本机 state.db 实测 20 份·最长 21891 字符 + 把 Kotlin 那条命令原样跑通 + 7 个驱动回归，共 115 条断言全 true）；`HP.BUILD=md-20260921-A`，APK sha256 `06710a7b…4182d678e` | 已修（本地测试台 + 命令原样跑通；**真机复验待做**） |
| R-05 | 能修改模型 | 只能发 `/model` 走 TUI（`tui.js:499`），App 内无读写路径；直接改远端 YAML 又有改坏的风险 | 三条只读/只写 op：`hermes.model`（读 `config.yaml` 的 `model` 段 + 备份清单）、`hermes.model.set`（**改前备份** → 只替换 `model.default` 一行 → 写回后**读回校验**并用 yaml 解析一遍）、`hermes.model.undo`（从备份还原）；界面 Hermes 页加「模型」一行，小窗里字段可编辑 + 候选一键填入 + 〔写回并校验〕〔还原上次〕 | 2026-09-21 md-B | **已修**｜`t-model.mjs` 14/14；另把 `Bridge.kt` 里的四段远端 python 原样抽出跑通（含临时 HOME 上试写 + 还原、yaml 解析 ok）。APK sha256 `ed6a04e032e95b2a939a9fb9f93080a32376fd3fba33210bf780b436affc17ab` |
| R-06 | 启动要挑 tmux：有就不管、没有才建、**始终不终止 tmux**；要能看 session/在线状态并选择 | 原来只有一条 `startCmd` 字符串：连接后无脑重跑它（`app.js` 旧逻辑），远端本来有会话也会再建一个；没有任何"远端有哪些会话"的界面；也没有"有就不管"的判断 | ① 新增只读 op `tmux.list`（原生跑 `tmux list-sessions -F …`，**只回原始文本**，解析留在界面侧好测）；② 新 `assets/ui/sessions.js`：`parse()` 纯函数 + `bootstrap()` 启动流程（**有会话→attach，没有→按主机命令建，再没有→自建**）+ `attach/create`；③ 新「会话」栏目（状态一行字「在线：已连接 · 远端 tmux：N 个会话」+ 会话行：名字/窗口数/建了多久/最后活动/attach 中·空闲，**点行切过去**）；④ 首连与重连都走同一条启动流程；⑤ **代码里没有任何 kill 路径**（源码 grep 自检） | `evidence/会话与流程/F-SESS-6-会话流程-原始输出.txt`（12 条断言全 true + 4 个旧驱动回归 + kill 源码自检无命中）；`HP.BUILD=sess-20260921-A`，APK sha256 `73801404…bfa38477` | 已修（本地测试台；**真远端 tmux + 模拟器复验待做**） |
| R-07 | 多个功能要藏进左侧隐藏栏，点「三」选择栏目 | 原来是居中 overlay + 4 个页签写死在 HTML 里（`index.html`），**没有"有哪些功能"的登记表** —— 加功能只能再手写一个页签 | ① 新建 `registry.js`（功能登记表：10 项，一个功能一行：id/名称/集合/载体/入口/稳定标识/状态/对应测试）+ `HP.Registry.check()` 覆盖自检；② 新建 `ui.js`（渲染器层 `HP.UI.row/list/sheet/status`，两行制行规范一处说了算）；③ 新建左侧抽屉 `#drawer`（点「☰」滑入，集合 ▸ 栏目，点条目进板块），**栏目由登记表生成**、不手写；④ 抬头显示当前栏目；⑤ 没做的栏目（planned）不摆出来，只由自检点名 | `evidence/渲染与设置/F-UI-7-左侧隐藏栏-原始输出.txt`：抽屉条目 4 = 登记可用 4、集合「连接/Hermes」、行样式 14px/600 + 次行 12px + 行高 55（≥44）、右列 `flexBasis:auto/nowrap/clip` 且数值没被截断、4 个栏目逐个点开都对、点空白收起、未做的点不动；自检 `没归属/没挂上/没测` 全空、`界面空集合=[diag]`（诊断集合要等 M4）；回归 t-settings/t-scroll 11 条断言全 true | `HP.BUILD=uidx-20260921-C`，APK sha256 `60f720b7…8343f05` | 已修（本地测试台；**模拟器真机复验待做**） |
| R-08 | 统计历史的上行下行（参 cdp，可复制） | 只存累计值 `trafficCum`（`app.js:1442/1492`），**没有区间记录 → 分不了桶** | 三层照 cdp 拆开：① **采集改存区间** —— 每秒采样把这一段增量落成 `{t0,t1,up,down}`（有增量才记，空转不占位），环形缓冲 120 条，跟累计值同一个 30s 节流口落盘（键 `trafficSamples`）；② **统计一处** `HP.Net.bucketize(list, 每组)` → `{buckets, up, down, total, 占比[]}`；③ **绘图一处** `HP.Net.bars/pie`（纯 DOM/CSS，不引外部库）。流量小窗改用统一渲染入口 `HP.UI.sheet` + `live` 重画（原来手拼 HTML、每秒重排整块）；多两个动作：〔计数归零〕（只清本次会话）〔清空历史〕。老版本只存过一个累计总数（没有时间窗）→ **不伪造巨柱**，只当累计显示 | 2026-09-21 net-B | **已修**｜`t-traffic.mjs` 18/18（有增量才记区间 / 上限环形丢最旧 / 落盘与内存一致 / 坏 JSON 当空 / 柱数=组数 / **柱上两段数字之和=汇总** / 饼占比相加=100 / 空历史不画空图 / 归零与清空各管各 / 老累计值不伪造桶 / 登记表自检全空）+ 11 个驱动回归，共 241 条断言全 true。APK sha256 `af22293f…3db36a6` |
| R-09 | 调字号之类的设置**无效**；要有效且默认保持当前界面的状态 | **已定位（两个真因）**：① 界面存了**两份状态** —— 设置面板改的是 `Panels.prefs`，终端读的是 `App.prefs`，改完只更新面板那份，当场不生效；② `boot()` **先读偏好后建通道**，`rpcRaw` 直接 reject → 落进 catch 只读到 localStorage 兜底值 → 每次冷启动所有设置回默认（面板随后又从桥里读回真值，于是"显示一套、生效一套"）。另有一处：双指缩放只改内存不落盘 | 写收成一个入口 `App.setPref(k,v)`（内存→落盘→触发派生动作），`Panels.prefs` 改成只读转发 getter；`boot()` 改成**先 `connectTransport()` 再 `loadPrefs()`**；捏合结束落盘；设置页加一行回读「生效中：字号 20px · 终端 32×22 · 回滚 1000 行 · 已保存」 | `evidence/渲染与设置/F-UI-9-设置生效-原始输出.txt`（11 条断言全 true）+ 测试台说明同目录；`HP.BUILD=uidx-20260921-A`，APK sha256 `11c6de11…41c6b2c7` | 已修（本地测试台；**模拟器真机复验待做**） |
| R-10 | 连接总是说「新建立连接」，是不是每次都上传 key | **已定位（真因）**：`onHostKey()` 里「首次连接，需要确认主机指纹」那句提醒写在**函数开头**，于是**每次连接**（同一台已信任主机、指纹一致也算）都推一条通知 —— 用户看到的"总是说新建立连接"就是它（实测修复前：已信任主机一栏 `app.notify=1`）。另外「原生 SSH 通道就绪」每次连接都弹一次也贡献了噪声。**「是不是每次上传 key」的答案：不是** —— 只有主机还记着"用密码登录"时第一次连接会调 1 次 `key.install`，装好并验证通过后主机切成密钥登录，第二次连接不再调（实测 1 → 0） | ① 提醒只放在真要人决定的分支（指纹变了 / 新密钥类型 / 真·首次），已知主机一致时**连提醒都不发**（留日志）；② 「通道就绪」改成本次运行只说一次；③ 设置项 `autoInstallKey` 的 hint 直说"之后不再装" | `evidence/会话与流程/F-SESS-10-连接提示与公钥-原始输出.txt`（修复前对照 + 修复后 7 条断言全 true：已信任主机静默 / 首次会问 / 指纹变了会问 / 新密钥类型会问 / 第一次会装公钥 / 装完切密钥 / 第二次不再装；另有 6 个驱动回归，共 108 条断言全 true）；`HP.BUILD=sess-20260921-C`，APK sha256 `6d26b6a9…edd2ca1` | 已修（本地测试台；**真机复验待做**） |
| R-11 | 加载完 skill 之后还要等很久才出聊天界面 | **已定位**：① 启动路径上**没有**远端读取（实测启动期间 `hermes.info` 调用数 0），能打字的时刻是 `HP.BOOT['终端可显示']=103ms`（面板就绪 127ms）；② 真正的浪费在 **Hermes 页**：切到它时 `renderHermes` 被调用两次（点栏目一次、`renderAll` 一次）→ 同一次读取发两遍；每次切回来都**强制刷新**（`renderHermes(true)`）→ 来回切就一直是「读取中…」；超时给到 30s/25s，远端慢时要对着空页面等半分钟 | ① 启动打点（`HP.BOOT` 六阶段，页面里可读）；② `_fetchHermes()` 记住 promise，**同一次读取只发一遍**；③ 60 秒缓存，点栏目不再强制刷新（要最新就点页里的「刷新」）；④ 超时收到 12s/10s；⑤ 抬头显示**读取耗时** | `evidence/渲染与设置/F-UI-11-启动时间线-原始输出.txt`（7 条断言全 true：启动 0 次远端读取、第一次进 1 次、切回来命中缓存仍 1 次、确有耗时行、技能真渲染） | `HP.BUILD=uidx-20260921-D`，APK sha256 `ce948fb7…f12a23e` | 已修（本地测试台；**模拟器真机复验待做**） |
| R-12 | 要能监听聊天输出，识别「结束 / 压缩中 / 运行中」 | 原来只有 `TuiWatcher` 判 `alt/busy/title`（判断的是"是不是 TUI、有没有鼠标"），**没有聊天语义**：不知道那一轮在跑、在压上下文、还是已经结束 | 新增 `assets/ui/chatstate.js`：**规则表**（每条规则带依据文案与出处）+ 状态机（`feed()` 喂真实渲染文本、`tick()` 用静默时长判结束、`explain()` 打印"凭什么这么判"）；接进 `renderChunk`（唯一输入）与每秒 tick；顶栏一行字显示状态、长按菜单「ℹ 聊天状态」可查依据；后台结束推通知（新设置项 `notifyChatState`） | `evidence/会话与流程/F-SESS-12-聊天状态机-原始输出.txt`（15 条断言全 true：7 条真实文案逐条判定、迁移序列 运行中→压缩中→运行中→已结束、静默阈值、真实链路推输出→顶栏文字、规则覆盖率 7/7）+ `规则依据-来自Hermes源码.txt`（文案 grep 自 ui-tui/dist，非猜测）；`HP.BUILD=sess-20260921-B`，APK sha256 `c281f66d…3a1c7060` | 已修（本地测试台；**真机 + 真 Hermes TUI 校准待做**） |
| R-13 | 断线之后点「连接」连不回去，只提示「打开会话失败: 传输未就绪」 | `App.connect()` 直接 `rpc('session.open')`；传输层对象还在但 `alive === false`，`rpcRaw` 第一行就 reject。重开传输只写在 `reconnectNow()`（「🔌 重新连接」）里，两个入口行为不一致 —— 网络掉线 / 远端重启 / 手动断开之后都会掉进这个状态 | `connect()` 发 `session.open` 之前，若传输层不活先 `transport.open()`（与「重新连接」走同一条路） | `evidence/设备复验/设备复验-原始输出.txt`（第四批 `确保连接`：修前卡 30s 停在 disconnected，修后 `disconnected → connected`） | 2026-09-21 dev-B | **已修**（真设备上复验通过） |
| R-18 | 没连接的时候点「☰」，弹一条像错误的提示 | 我在开抽屉时跑"功能自检"，把**还没渲染**（Hermes 那几行要连上才渲染）当成"栏目没挂上"报给用户 | ① 自检分清「未渲染」与「没挂上」（`Registry.check()` 多返回 `notRendered`，只有真挂不上才算缺）；② 开抽屉**不再弹**自检结果，只进控制台；③ 另立 `t-noconn.mjs`（7 条断言：不抛异常、无控制台错误、抽屉照开、提示里没有错误字样、那三行算未渲染不算没挂上） | `evidence/设备复验与源码清理/复验-原始输出.txt` | 2026-09-21 | **已修** |
| R-19 | 点终端那块屏幕**仍然弹键盘**（真机上；本地台验过"焦点不落隐藏输入框"） | 只挡"焦点落在隐藏输入框"在真机不够：WebView 在触摸之后还会主动给它一次 focus | 加**第二道总闸**：没按「⌨ 键盘」时隐藏输入框 `inputmode="none"`（被聚焦也不弹键盘），按了改回 `text`；`t-keyboard.mjs` 增 3 条断言；设备端 `device-check.mjs` 用真触摸验 | `evidence/设备复验与源码清理/复验-原始输出.txt` | 2026-09-21 | **已修（待真手机手感确认）** |
| R-20 | 全屏（沉浸）模式要保留输入框；从全屏退出来后「☰」和上下滑动没反应 | ① `body.immersive` 把 `#composer` 一起藏了；② 进出沉浸的调用点各自 `setTimeout(resetGeometry,60)` 只算一拍、顶栏高度还是缓存的 | ① 沉浸只藏顶栏与功能键条，输入框保留；② 进出统一走 `toggleImmersive()`：缓存作废 + 0/120/400ms 三拍重算几何；③ 新驱动 `t-immersive.mjs`（7 条：沉浸里输入框在视口内、顶栏键条藏起来、退出后都回来、进出前后单指下滑都能翻历史、退出后点☰能开抽屉且落点真是按钮） | 同上 | 2026-09-21 | **已修**（本地台复现不出 ☰ 失效，已加判据 + 设备端真触摸复验） |
| R-21 | 输入框「攒着」切到「实时」时，内容要怎么处理、要不要自动补换行 | 原来切模式**无条件清空**输入框（等于把草稿丢了） | 切到「实时」：把框里攒着的那段**送进终端且不补回车**（回车是"提交"，由用户自己按）；切回「攒着」：**不动内容**。`t-composer.mjs` 增 2 条断言 | 同上 | 2026-09-21 | **已修**（回车语义随后按 R-24 定：一律不自动补回车） |
| R-22 | 源码里用中文当标识符（用户："怎么这么不规范"） | 我图省事把变量名/对象键写成中文 | App 源码（`ui/*.js`）标识符全改英文：`提醒→notifyAttention`、`分组→bucketSpan`、`时长/秒→elapsedMs/secs`、`攒着的→pendingText`、`对账→verdict`、`在测→testing`、`状态行→statusRows`、`段→parts`、`一位→round1`、`这一段→sliceArr`、`占比→share`；`Registry.check()` 返回键全英文（`total/readyCount/notRendered/ungrouped/emptyGroups/uiEmptyGroups/notMounted/untested/notDone`）。测试驱动也全部改成英文（20 个 `t-*` + `harness.mjs` + `dev-probe.mjs`），临时排查用的 28 个 `d-*.mjs` 移进 `evidence/临时排查脚本-存档/`，设备端只留一个模块化的 `device-check.mjs` | 全量回归 + 设备复验（同上一份证据） | 2026-09-21 | **已做**（注释与界面文案按规矩保留中文） |
| R-24 | 「回车不行」——取消自动补回车 | 攒着模式点「发送」原来发的是 `内容 + '\r'`，等于替用户按了回车（TUI 直接提交、shell 直接执行） | ① 「发送」改名「写入」，只把内容送进终端、**不带回车**；② 切到「实时」那一下也是直接写进去、不补回车；③ 要回车只能由用户显式点功能键条上的「⏎」；④ `t-composer.mjs` 14 条断言：点「写入」发出的是 `mk`（无 `\r`/`\n`）、切模式不补回车、**键条 ⏎ 仍能发 `[[13]]`**（显式回车这条路必须留） | `evidence/取消自动回车/取消回车-原始输出.txt` | 2026-09-22 | **已做** |
| R-23 | 改名漏改：点「网络 → 原始输出」小窗报 `段 is not defined` | 把 `const 段` 改成 `parts` 时漏了同函数里的两处 `段.push` 与一处 `段.join` | 全部改成 `parts`；`t-net.mjs` 从 17/18 回到 **18/18** | 同上 | 2026-09-21 | **已修** |

| R-17 | 功能覆盖自检报出三条「没挂上」（系统提示词 / 模型 / 技能详情），看着像真缺 | `HP.Registry.check()` 的判据是**读 DOM**（栏目 tab、宿主栏目里那一行）。初次统一验收时**只渲染了部分栏目**就读，于是把"还没渲染"读成"没挂上" | 先渲染全部栏目（Hermes 那几行还要 `sessionId/state` 置成"已连接"）再读；`t-drawer.mjs` 改成先渲染再自检，另立 `t-registry.mjs` 专门盯这条（8 条断言） | `evidence/统一打包/功能覆盖自检-原始输出.txt`（同一屏两种读法的对照：只画一部分 → 没挂上=[prompt/model/skills-dl]，全部画完 → 缺口名单全空、登记表 11 项全挂上；抽屉 6 栏目与登记表一致） | 2026-09-21 unified | **已修（测试方法）** |
| R-16 | 「实时」模式下按一次回车，终端里**多出一个空回车**（TUI / shell 里等于多提交一次） | 输入框上挂了两个 keydown：①「攒着模式」用的 `Enter → sendComposer()`；② 实时模式的直通 `Enter → sendLive("\r")`。实时模式下两个都跑 ⇒ 一次回车发两遍 | ① 那个 Enter 只在**非实时**模式下接管（实时交给直通分支） | `evidence/键盘与保持/输入框实时与攒着-原始输出.txt`（t-composer 12 条断言：用**字符码**读回 —— 回车 `[[13]]` 只发一次、退格 `[[127]]`、逐字直通、中文上屏补发一次、攒着模式才留在框里、输出不落输入框、切模式不丢草稿；含 15 个驱动 347 条断言全 true） | 2026-09-21 kbd-B | **已修** |
| R-15 | 点上面那块屏幕（终端）会弹键盘，挡着复制 / 长按 / 拖动 | 三条真会叫出键盘的路：① xterm 自带的隐藏输入框（1×1 像素、压在光标位置）会被点中并聚焦（`pointer-events: none` 只挡住"点中谁"，挡不住 WebView 在**触摸之后**主动给的那次 focus —— 设备端实测点完 `activeElement` 仍是 `TEXTAREA.xterm-helper-textarea`）；② 已经开着的键盘不会因为点终端而收起；③ 设置里「点击终端弹键盘」开了也不弹（`term.focus()` 被 pointerup 之后的浏览器默认焦点行为盖掉） | ① 给隐藏输入框 `pointer-events: none`；② **焦点守卫**：没按「⌨ 键盘」时谁把焦点塞给它就立刻收回（规矩：只有「⌨ 键盘」按钮能叫出键盘）；③ 点终端时 `wantKeyboard(false)` + 把焦点从输入框收回（blur 不清内容，草稿不丢）；④ 开关那条改成下一拍再 focus | `evidence/键盘与保持/键盘与保持-原始输出.txt`（本地 11 条断言用**真实鼠标点** + 8 条"对话框攒着"断言 + 设备端真触摸点（点终端→焦点 BODY；对照点输入框→焦点落到 cinput）+ 13 个驱动 316 条断言全 true） | 2026-09-21 kbd-A | **已修**（设备端复验通过） |
| R-14 | 真机上点技能行「读技能失败：读不到 … skills\/x\/SKILL.md」；回写内容会变形 | 拿 Android 的 `JSONObject.quote()` 去拼 **python 字符串字面量**：org.json 会把 `/` 转义成 `\/`（JSON 合法、JSON 解码器会还原），python 不认这个转义 → 反斜杠原样留在字符串里。路径被写坏读不到文件；同一处还会把 base64 里的 `/`、带斜杠的模型名写坏 | `Bridge.kt` 新增 `pyStr()`（只转义 python 认的字符），4 处拼字面量全换掉，并注明为什么不能用 `JSONObject.quote()` | `evidence/设备复验/设备复验-原始输出.txt`（第四批：同一路径修后读到 `@@SIZE 343 / @@SHA 56a0baf…`；下载到手机 sha256 与远端一致；回写 358 字节 / 新 sha `78eeed157c10`，宿主侧核对远端文件与备份都对得上） | 2026-09-21 dev-B | **已修**（真设备上复验通过） |
| F1 | 设置页「构建版本」那一行对不上本包：页面 `HP.BUILD=unified-20260921c`、包内 `builtAt=2026-09-22 00:35`，而那个包是 9-22 23:33 出的、还含当晚才改的 `talk.js`（测试者第 1 步报的失败项） | 版本串与打包时间**靠人手写**：出包（`tools/build.sh`）只跑 gradle，没人去改 `build-info.json`；`stamp-build.py` 只把 JSON 盖进 `app.js`，JSON 里的时间戳得人记得更新 —— 忘了就出现「设备上跑的这版还写着上一包」 | 出包时**自动写**：`stamp-build.py` 现按出包时刻生成 `testVersion`（轮次名-日期-时分秒，如同一天出几包也不重名）与 `builtAt`，再盖进 `ui/app.js`；`build.sh` 变成 ①盖章 ②核对 ③gradle ④解开包对账（包内 `build-info.json` / `HP.BUILD` / `ui` 每个文件与源树比哈希）⑤归档 —— **对账不过就不归档**；新增 `stamp-build.py --verify <apk>` 供测试者复用。顺带修掉三处写死 `/home/lwgat/hermes-pocket` 的路径（改成按脚本位置推，换机器/换目录都能用） | `evidence/测试轮-20260923/F1-版本串每包更新-20260923.txt`（旧包对账 FAIL 复现 F1 / 新包 PASS / 页面读数 ↔ 包内读数三方对照 / 手改版本串被 `--check` 拦住并逐字节复原）、同目录 `t-pack-原始输出-20260923.json`；构件 `apk/测试版/hermes-pocket-版本串每包更新-20260923.apk` sha256 `75127e45cdb32207fa7db268bc08829b163f9ae8f4408b93106e8a9760eab408`，`HP.BUILD=unified-20260923-004612` | **已修**（本地测试台 7/7 + 包内对账 PASS；**设备上读数等第 3 步复测**） |

### 本轮新增（本人 2026-09-23 口述，六条；登记照旧：现象 / 根因 / 修法 / 证据 / 状态）

> 排期：**先把要求写下来，等经理有空再排**（本人原话「这些功能让经理慢慢安排，不需要操之过急」）。
> 状态一律先记 `待做`；每条开工前先把"根因"补成**已定位**（要能指出是哪一行/哪条路径），再写修法。

| 编号 | 现象（用户原话式） | 根因 | 修法 | 证据 | 状态 |
|---|---|---|---|---|---|
| R-25 | **省电模式优化** | 待定位（要量：长连接心跳、每秒 tick 重绘、轮询、后台是否还在跑） | 待方案：给"省电档"（前台/后台/熄屏各自的心跳与重绘策略），并给出可测的耗电对照 | 待做 | **待做**（已登记，等排期） |
| R-26 | **自动本地存储优化** | 待定位（本地都存了什么、多大、什么时候落盘、满了怎么办；含 localStorage 膨胀与终端回滚缓冲） | 待方案：容量上限 + 落盘节流 + 自动清理策略，且要能读回"现在占多少" | 待做 | **待做** |
| R-27 | **网络统计不全** | 待定位（现有「网络」栏只有 ping/端口耗时 + 区间上下行；缺什么要逐项列出来：按天/按会话、丢包历史、连接次数、失败率…） | 待方案：先出"该统计什么"的清单，再补采集与展示 | 待做 | **待做** |
| R-28 | **聊天历史优化** | 待定位（历史条数上限、加载方式、搜索、清空策略） | 待方案 | 待做 | **待做** |
| R-29 | **群聊不自动到最下面** | **已定位（2026-09-23）**：钉底只做在**单聊**一条路上（`talk.js:884` `box.scrollTop = box.scrollHeight`，容器 `#tk-chat`），**群聊那条路一处都没有**：① 新消息到达走 `tick()`（`talk.js:190-196`，2.5s 轮询）→ `paintStream()`（`talk.js:510-530`）把 `#tk-stream` 整块重建后不设 `scrollTop`；② 切栏目进群聊走 `onShow`（`talk.js:128`）→ `render()`（`200-205`）→ `paintGroup()`（`367-377`）→ 同一个 `paintStream()`，同样不钉底；③ 全文没有「在不在底部 / 用户有没有上翻」的状态（grep `scrollTop` 只命中 `884` 一处）。本地测试台实测（跑真 `paintGroup`/`paintStream`、读真 DOM）：进群聊 `scrollTop=0`、离底部 3386px，头顶是最老的第 1 条；新消息重画后仍 `scrollTop=0`、最后一条不在可视区；单聊对照离底部 0（钉底） | 方案（钉底收成一个入口）：加 `pinBottom(box,{force})` —— 重画前先判「在不在底部」（`scrollHeight - scrollTop - clientHeight <= 24`），只在底部或 `force`（切栏目/首渲）时置 `scrollTop = scrollHeight`；三处调用点（`paintStream` 新消息、`paintGroup` 切栏目、`paintChannel` 频道重建）接同一入口，单聊 `884` 也改走它；`paintStream` 不再整块 `textContent=''`，改成按 `m.id` 只追加新行（上翻时位置天然不跳）；上翻后出现「⇣ 回到底部（新 N 条）」入口，点它到底并清计数。**必须保住**现在「上翻不被抢回」的行为（实测上翻到中间后重画位置没被动）。出包归 renderer：`bash tools/build.sh --name "R29-群聊钉底" --feature "…" --feature-id "R-29"`（本轮无新增依赖，纯 `assets/ui/talk.js`） | `evidence/R29-定位-20260923/R-29-定位-原始输出.txt`（文件与行 + 探针原始 JSON + 7 条判据，sha256 `31779c08ad280734…`）、探针 `source/hermes-pocket/tools/ui-harness/d-r29-scroll.mjs` | **已定位**（真点由 tester 在本轮第 3 步做；未验 ≠ 通过） |
| R-32 | 单聊页「他在会话里回的话」不会再自动冒出来（顺带发现，2026-09-23） | `talk.js` 里 `this.pullRoleOutput(...)` **4 处调用、0 处定义**（调用在 `135`、`850`、`851`、`885`）；定义是在 `f8f031d`（1:1 整段重写）里被删掉的，`ee40d76`「恢复 paintChat」把调用恢复了、函数没恢复 → 单聊页每 2.5s 一条 unhandled rejection，两处延时拉取全抛 | 待做（先把函数恢复回去，或把四处调用改成真正存在的入口） | 同上证据文件 §⑤ | **待做** |
| R-30 | **聊天消息客户端本地化** | 口径待确认（"本地化"= 消息落本地库/离线可看，还是界面文案本地化）——**先跟本人确认再动手** | 待方案 | 待做 | **待做** |
| R-31 | **发送消息的迟滞以及反馈**（本人 2026-09-23 口述：两种读法都要覆盖） | **已定位（2026-09-23）**：① **界面侧** —— `talk.js:895-906` `send()` 只 `await rpc('talk.say')`（`899-900`）**不看回执**，紧接着 `talk.js:901` 一句 `toast('已发出')` 就结束；而两端其实都回了回执：原生 `Bridge.kt:754-765` 回 `{to,kind,delivered:bool,raw}`、平台 `talk.py:815-818` 回 `{id,delivered[,error]}` —— **回执没人读**；界面也没有 per-message 状态（`Talk` 上无 ack/state 字段，`HP.App.toast` 默认 1900ms 自动消失）。② **平台侧** —— `talk.py:880-925` `deliver()`：多行固定 `sleep 0.8`(`915`) + `sleep 0.6`(`917`) 再补一次 Enter（`check=False`），单行 `sleep 0.12`(`922`)；广播是**串行**逐角色投（`800-806`）⇒ 每多一个目标约 +1.0~1.4s。`delivery` 表只有 `(msg_id, role, at, ok, note)`（`talk.py:66`），**没有耗时列**、`at` 只到整秒；`talk.py:1280-1290` 的「没回音」判定拿 `ok=1` 当真送达，而 `ok=1` 只等于「tmux 命令没报错」 | 方案：界面加发送状态机三态（发送中 / 已送达 ◯◯ms / 没送达:原因）+ 8s 超时与重试，状态挂在该条消息上（不增常驻按键）；平台 `deliver()` 前后计时、`delivery` 加 `ms` 列、`say()` 把每目标 ms 与结果回给界面，并把 `ok` 分成「命令成功 / 读回确认」；新增设置项 `sendTiming`（默认开，省电/省流量档默认关、只留终态）。出包归 renderer：`bash tools/build.sh --name "R31-发送反馈" --feature "…" --feature-id "R-31"` | `evidence/R31-定位-20260923/R-31-定位-原始输出.txt`（链路 `file:line` + 拦桥探针原始 JSON + 平台台账实测差值表 + 8 条判据，sha256 `6fddf64df7ed1511…`）、探针 `source/hermes-pocket/tools/ui-harness/d-r31-send.mjs` | **已定位**（真点由 tester 在第 3 步做；未验 ≠ 通过） |

## 环境与归档约束（做事时别忘）

- 构建/测试：容器 `notifbridge-emu`（无头模拟器 + adb），依赖走本地镜像；**先停模拟器腾内存再构建**，重活串行。
- 工作副本 / 真源 = `/vol1/1000/airesults/hermes-pocket/source/hermes-pocket/`（git 仓库在 `/vol1/1000/airesults/hermes-pocket/`）。
  **`/home/lwgat/hermes-pocket/` 是 9-21 的旧副本**（连 `ui/talk.js` 都没有），别再从它构建或测界面 —— 界面测试台与 `t-pack.mjs` 原来写死的那条路径已改成按脚本位置推。
- 证据存 `evidence/<主题>/`，脚本存 `tools/验收脚本/`；构件哈希每次更新。
