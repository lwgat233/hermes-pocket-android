# 问题与需求登记（Hermes Pocket · 第二轮）

格式：**编号 / 现象（用户原话式） / 根因 / 修法 / 证据 / 状态**。状态只有三个：待做 / 在做 / 已修（附证据路径）。
用户报的每一条都进这里；做完不许删除，只改状态并补证据路径。

| 编号 | 现象（用户原话式） | 根因 | 修法 | 证据 | 状态 |
|---|---|---|---|---|---|
| R-01 | 长文本没法往下拉；要能一键往下拉；要能限制缓存文本长度 | **已定位**：① 普通缓冲其实能滚（实测 300 行：下滑翻开 17 行、出现「⇣回到底部」、点它回到最新、入口消失）；"拉不动"主要在 **TUI（备用屏）**——本地没有历史，滚轮必须发给远端，而 tmux 默认没开鼠标；而且**滚上去之后没有回程的入口**（旧逻辑在备用屏里按 `baseY-viewportY` 算，实测恒为 1，显示成没用的「上面还有 1 行」）。② 「缓存上限」改了不生效 = R-09 的同一根因（两份状态 + 启动顺序） | ① 备用屏按"往外发了几格滚轮"给「⇣ 回到最新（TUI 里已往上翻 N 格）」入口，点它**反向发滚轮**把视图还回去；进出 TUI 时刷新入口文案（不再显示 `上面还有 N 行`）；② 长按终端菜单加一项「⇣ 回到底部 / 最新」（常驻按键不增）；③ 远端没开鼠标时仍然一次会话只问一次，不假滚 | `evidence/渲染与设置/F-UI-1-长文本滚动-原始输出.txt`（12 条断言全 true：普通 4 + 备用屏 8，另一路"没鼠标"验证弹出了「开启 tmux 鼠标 / 取消」）；`HP.BUILD=uidx-20260921-B`，APK sha256 `a6a77c56…f856687f` | 已修（本地测试台；**模拟器真机复验待做**） |
| R-02 | 要有连通性测试、丢包率 | 功能不存在（只有终端里手打 ping） | 新栏目「网络」（诊断集合，登记表生成）：① `net.ping` 远端跑 `LC_ALL=C ping -c N -W 2`（全丢包也用 `\|\| true` 保住输出，不让它变成一个没内容的异常）；② `net.tcp` 远端 python 建一次连接量耗时；③ 解析全在界面侧 `net.js`（纯函数）——**包数 / 丢包率 / 最小-平均-最大 RTT / 通不通 / 耗时 ms**；④ 行上一个测试项，点行就跑、跑完把数字写回行里；⑤ 要看原文点「原始输出」行（ping 与端口两段 + 远端耗时） | 2026-09-21 net-A | **已修**｜`t-net.mjs` 18/18（解析喂的是 `tools/check_net_py.py` 把 `Bridge.kt` 里命令**原样抽出来跑**抓的真实输出：回环 0% 丢包 / 100% 丢包 / 域名解析失败 / 端口通 / 端口被拒；含界面流程与 F-NET-8 统计纯函数）+ 10 个驱动回归，共 178 条断言全 true。APK sha256 `cbd9c0f1…d7bc2dd`。样本文件 `evidence/网络与流量/fixtures/net-samples.json` |
| R-03 | 能把 skill 下载到本地、能修改 | 原来只能读 + 在终端 `less` 打开（`panels.js:376 openSkill`），没有下载/回存；手机上要"改"只能整篇重打 | 技能行改由统一渲染器 `HP.UI.sheet` 打开（M1 的单一渲染入口）：① 只读 op `skill.read` 取**原文**（回带 sha256 + 字节数 + base64 正文，字节可对账）；② 〔下载到手机〕走新原生 op `local.save`——MediaStore 落 `Download/hermes-skills/`（Android 10+ 免权限；失败退 App 外部目录并如实报真实路径），**写完读回算 sha256**，与远端 sha 一致才在提示里说「与远端一致」；③ 〔编辑并回写〕走 `skill.write`：**带打开那一版的 sha256 对账**（远端被别人改过就拒绝，不静默覆盖）→ 改前备份 → 原子替换 → 读回算 sha256/字节数 | 2026-09-21 md-C | **已修**｜`t-skill.mjs` 23/23（sha256 全是真算：页面 WebCrypto + 驱动 node crypto）+ `tools/check_skill_py.py`（把 `Bridge.kt` 里两段远端 python 原样抽出跑：读 sha/size/正文一致；写→对账/备份/读回；**旧 sha 再写必须被拒**；共 167 条断言全 true）。APK sha256 `74db1ffc…c4781ab`。**手机侧 MediaStore 写入只能真机验**，测试台验的是名字/内容/sha 对账与失败如实报错 |
| R-04 | 能知道系统提示词等信息 | Hermes 页原来只有技能 + 记忆；系统提示词在远端是**代码组装**的，App 里没有任何来源；硬拼一份"像的"出来就是假证 | 新增只读 op `hermes.prompt`：远端用 python3 只读打开 `state.db`，从 `system_prompts(hash→prompt)` 取「最近会话用过的那一份」（`sessions.system_prompt_hash` 关联），回带标记的原始文本；界面侧 `assets/ui/remote.js` 解析成结构化数据，Hermes 栏目加一行「系统提示词」（次行写份数与长度、点行开小窗看正文、可看更多/复制）；取不到就如实说原因 | `evidence/Hermes远端/F-HERM-4-系统提示词-原始输出.txt`（11 条断言全 true：解析真实输出 / 坏数据如实报错 / 行渲染合规 / 抬头写清范围 / 来源写清在 state.db / 看更多真取下一段 / 到底了说已到底 / 取不到不装样子；另含本机 state.db 实测 20 份·最长 21891 字符 + 把 Kotlin 那条命令原样跑通 + 7 个驱动回归，共 115 条断言全 true）；`HP.BUILD=md-20260921-A`，APK sha256 `06710a7b…4182d678e` | 已修（本地测试台 + 命令原样跑通；**真机复验待做**） |
| R-05 | 能修改模型 | 只能发 `/model` 走 TUI（`tui.js:499`），App 内无读写路径；直接改远端 YAML 又有改坏的风险 | 三条只读/只写 op：`hermes.model`（读 `config.yaml` 的 `model` 段 + 备份清单）、`hermes.model.set`（**改前备份** → 只替换 `model.default` 一行 → 写回后**读回校验**并用 yaml 解析一遍）、`hermes.model.undo`（从备份还原）；界面 Hermes 页加「模型」一行，小窗里字段可编辑 + 候选一键填入 + 〔写回并校验〕〔还原上次〕 | 2026-09-21 md-B | **已修**｜`t-model.mjs` 14/14；另把 `Bridge.kt` 里的四段远端 python 原样抽出跑通（含临时 HOME 上试写 + 还原、yaml 解析 ok）。APK sha256 `ed6a04e032e95b2a939a9fb9f93080a32376fd3fba33210bf780b436affc17ab` |
| R-06 | 启动要挑 tmux：有就不管、没有才建、**始终不终止 tmux**；要能看 session/在线状态并选择 | 原来只有一条 `startCmd` 字符串：连接后无脑重跑它（`app.js` 旧逻辑），远端本来有会话也会再建一个；没有任何"远端有哪些会话"的界面；也没有"有就不管"的判断 | ① 新增只读 op `tmux.list`（原生跑 `tmux list-sessions -F …`，**只回原始文本**，解析留在界面侧好测）；② 新 `assets/ui/sessions.js`：`parse()` 纯函数 + `bootstrap()` 启动流程（**有会话→attach，没有→按主机命令建，再没有→自建**）+ `attach/create`；③ 新「会话」栏目（状态一行字「在线：已连接 · 远端 tmux：N 个会话」+ 会话行：名字/窗口数/建了多久/最后活动/attach 中·空闲，**点行切过去**）；④ 首连与重连都走同一条启动流程；⑤ **代码里没有任何 kill 路径**（源码 grep 自检） | `evidence/会话与流程/F-SESS-6-会话流程-原始输出.txt`（12 条断言全 true + 4 个旧驱动回归 + kill 源码自检无命中）；`HP.BUILD=sess-20260921-A`，APK sha256 `73801404…bfa38477` | 已修（本地测试台；**真远端 tmux + 模拟器复验待做**） |
| R-07 | 多个功能要藏进左侧隐藏栏，点「三」选择栏目 | 原来是居中 overlay + 4 个页签写死在 HTML 里（`index.html`），**没有"有哪些功能"的登记表** —— 加功能只能再手写一个页签 | ① 新建 `registry.js`（功能登记表：10 项，一个功能一行：id/名称/集合/载体/入口/稳定标识/状态/对应测试）+ `HP.Registry.check()` 覆盖自检；② 新建 `ui.js`（渲染器层 `HP.UI.row/list/sheet/status`，两行制行规范一处说了算）；③ 新建左侧抽屉 `#drawer`（点「☰」滑入，集合 ▸ 栏目，点条目进板块），**栏目由登记表生成**、不手写；④ 抬头显示当前栏目；⑤ 没做的栏目（planned）不摆出来，只由自检点名 | `evidence/渲染与设置/F-UI-7-左侧隐藏栏-原始输出.txt`：抽屉条目 4 = 登记可用 4、集合「连接/Hermes」、行样式 14px/600 + 次行 12px + 行高 55（≥44）、右列 `flexBasis:auto/nowrap/clip` 且数值没被截断、4 个栏目逐个点开都对、点空白收起、未做的点不动；自检 `没归属/没挂上/没测` 全空、`界面空集合=[diag]`（诊断集合要等 M4）；回归 t-settings/t-scroll 11 条断言全 true | `HP.BUILD=uidx-20260921-C`，APK sha256 `60f720b7…8343f05` | 已修（本地测试台；**模拟器真机复验待做**） |
| R-08 | 统计历史的上行下行（参 cdp，可复制） | 只存累计值 `trafficCum`（`app.js:1442/1492`），**没有区间记录 → 分不了桶** | 三层照 cdp 拆开：① **采集改存区间** —— 每秒采样把这一段增量落成 `{t0,t1,up,down}`（有增量才记，空转不占位），环形缓冲 120 条，跟累计值同一个 30s 节流口落盘（键 `trafficSamples`）；② **统计一处** `HP.Net.bucketize(list, 每组)` → `{buckets, up, down, total, 占比[]}`；③ **绘图一处** `HP.Net.bars/pie`（纯 DOM/CSS，不引外部库）。流量小窗改用统一渲染入口 `HP.UI.sheet` + `live` 重画（原来手拼 HTML、每秒重排整块）；多两个动作：〔计数归零〕（只清本次会话）〔清空历史〕。老版本只存过一个累计总数（没有时间窗）→ **不伪造巨柱**，只当累计显示 | 2026-09-21 net-B | **已修**｜`t-traffic.mjs` 18/18（有增量才记区间 / 上限环形丢最旧 / 落盘与内存一致 / 坏 JSON 当空 / 柱数=组数 / **柱上两段数字之和=汇总** / 饼占比相加=100 / 空历史不画空图 / 归零与清空各管各 / 老累计值不伪造桶 / 登记表自检全空）+ 11 个驱动回归，共 241 条断言全 true。APK sha256 `af22293f…3db36a6` |
| R-09 | 调字号之类的设置**无效**；要有效且默认保持当前界面的状态 | **已定位（两个真因）**：① 界面存了**两份状态** —— 设置面板改的是 `Panels.prefs`，终端读的是 `App.prefs`，改完只更新面板那份，当场不生效；② `boot()` **先读偏好后建通道**，`rpcRaw` 直接 reject → 落进 catch 只读到 localStorage 兜底值 → 每次冷启动所有设置回默认（面板随后又从桥里读回真值，于是"显示一套、生效一套"）。另有一处：双指缩放只改内存不落盘 | 写收成一个入口 `App.setPref(k,v)`（内存→落盘→触发派生动作），`Panels.prefs` 改成只读转发 getter；`boot()` 改成**先 `connectTransport()` 再 `loadPrefs()`**；捏合结束落盘；设置页加一行回读「生效中：字号 20px · 终端 32×22 · 回滚 1000 行 · 已保存」 | `evidence/渲染与设置/F-UI-9-设置生效-原始输出.txt`（11 条断言全 true）+ 测试台说明同目录；`HP.BUILD=uidx-20260921-A`，APK sha256 `11c6de11…41c6b2c7` | 已修（本地测试台；**模拟器真机复验待做**） |
| R-10 | 连接总是说「新建立连接」，是不是每次都上传 key | **已定位（真因）**：`onHostKey()` 里「首次连接，需要确认主机指纹」那句提醒写在**函数开头**，于是**每次连接**（同一台已信任主机、指纹一致也算）都推一条通知 —— 用户看到的"总是说新建立连接"就是它（实测修复前：已信任主机一栏 `app.notify=1`）。另外「原生 SSH 通道就绪」每次连接都弹一次也贡献了噪声。**「是不是每次上传 key」的答案：不是** —— 只有主机还记着"用密码登录"时第一次连接会调 1 次 `key.install`，装好并验证通过后主机切成密钥登录，第二次连接不再调（实测 1 → 0） | ① 提醒只放在真要人决定的分支（指纹变了 / 新密钥类型 / 真·首次），已知主机一致时**连提醒都不发**（留日志）；② 「通道就绪」改成本次运行只说一次；③ 设置项 `autoInstallKey` 的 hint 直说"之后不再装" | `evidence/会话与流程/F-SESS-10-连接提示与公钥-原始输出.txt`（修复前对照 + 修复后 7 条断言全 true：已信任主机静默 / 首次会问 / 指纹变了会问 / 新密钥类型会问 / 第一次会装公钥 / 装完切密钥 / 第二次不再装；另有 6 个驱动回归，共 108 条断言全 true）；`HP.BUILD=sess-20260921-C`，APK sha256 `6d26b6a9…edd2ca1` | 已修（本地测试台；**真机复验待做**） |
| R-11 | 加载完 skill 之后还要等很久才出聊天界面 | **已定位**：① 启动路径上**没有**远端读取（实测启动期间 `hermes.info` 调用数 0），能打字的时刻是 `HP.BOOT['终端可显示']=103ms`（面板就绪 127ms）；② 真正的浪费在 **Hermes 页**：切到它时 `renderHermes` 被调用两次（点栏目一次、`renderAll` 一次）→ 同一次读取发两遍；每次切回来都**强制刷新**（`renderHermes(true)`）→ 来回切就一直是「读取中…」；超时给到 30s/25s，远端慢时要对着空页面等半分钟 | ① 启动打点（`HP.BOOT` 六阶段，页面里可读）；② `_fetchHermes()` 记住 promise，**同一次读取只发一遍**；③ 60 秒缓存，点栏目不再强制刷新（要最新就点页里的「刷新」）；④ 超时收到 12s/10s；⑤ 抬头显示**读取耗时** | `evidence/渲染与设置/F-UI-11-启动时间线-原始输出.txt`（7 条断言全 true：启动 0 次远端读取、第一次进 1 次、切回来命中缓存仍 1 次、确有耗时行、技能真渲染） | `HP.BUILD=uidx-20260921-D`，APK sha256 `ce948fb7…f12a23e` | 已修（本地测试台；**模拟器真机复验待做**） |
| R-12 | 要能监听聊天输出，识别「结束 / 压缩中 / 运行中」 | 原来只有 `TuiWatcher` 判 `alt/busy/title`（判断的是"是不是 TUI、有没有鼠标"），**没有聊天语义**：不知道那一轮在跑、在压上下文、还是已经结束 | 新增 `assets/ui/chatstate.js`：**规则表**（每条规则带依据文案与出处）+ 状态机（`feed()` 喂真实渲染文本、`tick()` 用静默时长判结束、`explain()` 打印"凭什么这么判"）；接进 `renderChunk`（唯一输入）与每秒 tick；顶栏一行字显示状态、长按菜单「ℹ 聊天状态」可查依据；后台结束推通知（新设置项 `notifyChatState`） | `evidence/会话与流程/F-SESS-12-聊天状态机-原始输出.txt`（15 条断言全 true：7 条真实文案逐条判定、迁移序列 运行中→压缩中→运行中→已结束、静默阈值、真实链路推输出→顶栏文字、规则覆盖率 7/7）+ `规则依据-来自Hermes源码.txt`（文案 grep 自 ui-tui/dist，非猜测）；`HP.BUILD=sess-20260921-B`，APK sha256 `c281f66d…3a1c7060` | 已修（本地测试台；**真机 + 真 Hermes TUI 校准待做**） |
| R-13 | 断线之后点「连接」连不回去，只提示「打开会话失败: 传输未就绪」 | `App.connect()` 直接 `rpc('session.open')`；传输层对象还在但 `alive === false`，`rpcRaw` 第一行就 reject。重开传输只写在 `reconnectNow()`（「🔌 重新连接」）里，两个入口行为不一致 —— 网络掉线 / 远端重启 / 手动断开之后都会掉进这个状态 | `connect()` 发 `session.open` 之前，若传输层不活先 `transport.open()`（与「重新连接」走同一条路） | `evidence/设备复验/设备复验-原始输出.txt`（第四批 `确保连接`：修前卡 30s 停在 disconnected，修后 `disconnected → connected`） | 2026-09-21 dev-B | **已修**（真设备上复验通过） |
| R-18 | 没连接的时候点「☰」，弹一条像错误的提示 | 我在开抽屉时跑"功能自检"，把**还没渲染**（Hermes 那几行要连上才渲染）当成"栏目没挂上"报给用户 | ① 自检分清「未渲染」与「没挂上」（`Registry.check()` 多返回 `notRendered`，只有真挂不上才算缺）；② 开抽屉**不再弹**自检结果，只进控制台；③ 另立 `t-noconn.mjs`（7 条断言：不抛异常、无控制台错误、抽屉照开、提示里没有错误字样、那三行算未渲染不算没挂上） | `evidence/设备复验与源码清理/复验-原始输出.txt` | 2026-09-21 | **已修** |
| R-19 | 点终端那块屏幕**仍然弹键盘**（真机上；本地台验过"焦点不落隐藏输入框"） | 只挡"焦点落在隐藏输入框"在真机不够：WebView 在触摸之后还会主动给它一次 focus | 加**第二道总闸**：没按「⌨ 键盘」时隐藏输入框 `inputmode="none"`（被聚焦也不弹键盘），按了改回 `text`；`t-keyboard.mjs` 增 3 条断言；设备端 `device-check.mjs` 用真触摸验 | `evidence/设备复验与源码清理/复验-原始输出.txt` | 2026-09-21 | **已修（待真手机手感确认）** |
| R-20 | 全屏（沉浸）模式要保留输入框；从全屏退出来后「☰」和上下滑动没反应 | ① `body.immersive` 把 `#composer` 一起藏了；② 进出沉浸的调用点各自 `setTimeout(resetGeometry,60)` 只算一拍、顶栏高度还是缓存的 | ① 沉浸只藏顶栏与功能键条，输入框保留；② 进出统一走 `toggleImmersive()`：缓存作废 + 0/120/400ms 三拍重算几何；③ 新驱动 `t-immersive.mjs`（7 条：沉浸里输入框在视口内、顶栏键条藏起来、退出后都回来、进出前后单指下滑都能翻历史、退出后点☰能开抽屉且落点真是按钮） | 同上 | 2026-09-21 | **已修**（本地台复现不出 ☰ 失效，已加判据 + 设备端真触摸复验） |
| R-21 | 输入框「攒着」切到「实时」时，内容要怎么处理、要不要自动补换行 | 原来切模式**无条件清空**输入框（等于把草稿丢了） | 切到「实时」：把框里攒着的那段**送进终端且不补回车**（回车是"提交"，由用户自己按）；切回「攒着」：**不动内容**。`t-composer.mjs` 增 2 条断言 | 同上 | 2026-09-21 | **已修**（回车语义随后按 R-24 定：一律不自动补回车） |
| R-22 | 源码里用中文当标识符（用户："怎么这么不规范"） | 我图省事把变量名/对象键写成中文 | App 源码（`ui/*.js`）标识符全改英文：`提醒→notifyAttention`、`分组→bucketSpan`、`时长/秒→elapsedMs/secs`、`攒着的→pendingText`、`对账→verdict`、`在测→testing`、`状态行→statusRows`、`段→parts`、`一位→round1`、`这一段→sliceArr`、`占比→share`；`Registry.check()` 返回键全英文（`total/readyCount/notRendered/ungrouped/emptyGroups/uiEmptyGroups/notMounted/untested/notDone`）。测试驱动也全部改成英文（20 个 `t-*` + `harness.mjs` + `dev-probe.mjs`），临时排查用的 28 个 `d-*.mjs` 移进 `evidence/临时排查脚本-存档/`，设备端只留一个模块化的 `device-check.mjs` | 全量回归 + 设备复验（同上一份证据） | 2026-09-21 | **已做**（注释与界面文案按规矩保留中文） |
| R-24 | 「回车不行」——取消自动补回车 | 攒着模式点「发送」原来发的是 `内容 + '\r'`，等于替用户按了回车（TUI 直接提交、shell 直接执行） | ① 「发送」改名「写入」，只把内容送进终端、**不带回车**；② 切到「实时」那一下也是直接写进去、不补回车；③ 要回车只能由用户显式点功能键条上的「⏎」；④ `t-composer.mjs` 14 条断言：点「写入」发出的是 `mk`（无 `\r`/`\n`）、切模式不补回车、**键条 ⏎ 仍能发 `[[13]]`**（显式回车这条路必须留） | `evidence/取消自动回车/取消回车-原始输出.txt` | 2026-09-22 | **已做** |
| R-23 | 改名漏改：点「网络 → 原始输出」小窗报 `段 is not defined` | 把 `const 段` 改成 `parts` 时漏了同函数里的两处 `段.push` 与一处 `段.join` | 全部改成 `parts`；`t-net.mjs` 从 17/18 回到 **18/18** | 同上 | 2026-09-21 | **已修** |

| R-17 | 功能覆盖自检报出三条「没挂上」（系统提示词 / 模型 / 技能详情），看着像真缺 | `HP.Registry.check()` 的判据是**读 DOM**（栏目 tab、宿主栏目里那一行）。初次统一验收时**只渲染了部分栏目**就读，于是把"还没渲染"读成"没挂上" | 先渲染全部栏目（Hermes 那几行还要 `sessionId/state` 置成"已连接"）再读；`t-drawer.mjs` 改成先渲染再自检，另立 `t-registry.mjs` 专门盯这条（8 条断言） | `evidence/统一打包/功能覆盖自检-原始输出.txt`（同一屏两种读法的对照：只画一部分 → 没挂上=[prompt/model/skills-dl]，全部画完 → 缺口名单全空、登记表 11 项全挂上；抽屉 6 栏目与登记表一致） | 2026-09-21 unified | **已修（测试方法）** |
| R-16 | 「实时」模式下按一次回车，终端里**多出一个空回车**（TUI / shell 里等于多提交一次） | 输入框上挂了两个 keydown：①「攒着模式」用的 `Enter → sendComposer()`；② 实时模式的直通 `Enter → sendLive("\r")`。实时模式下两个都跑 ⇒ 一次回车发两遍 | ① 那个 Enter 只在**非实时**模式下接管（实时交给直通分支） | `evidence/键盘与保持/输入框实时与攒着-原始输出.txt`（t-composer 12 条断言：用**字符码**读回 —— 回车 `[[13]]` 只发一次、退格 `[[127]]`、逐字直通、中文上屏补发一次、攒着模式才留在框里、输出不落输入框、切模式不丢草稿；含 15 个驱动 347 条断言全 true） | 2026-09-21 kbd-B | **已修** |
| R-15 | 点上面那块屏幕（终端）会弹键盘，挡着复制 / 长按 / 拖动 | 三条真会叫出键盘的路：① xterm 自带的隐藏输入框（1×1 像素、压在光标位置）会被点中并聚焦（`pointer-events: none` 只挡住"点中谁"，挡不住 WebView 在**触摸之后**主动给的那次 focus —— 设备端实测点完 `activeElement` 仍是 `TEXTAREA.xterm-helper-textarea`）；② 已经开着的键盘不会因为点终端而收起；③ 设置里「点击终端弹键盘」开了也不弹（`term.focus()` 被 pointerup 之后的浏览器默认焦点行为盖掉） | ① 给隐藏输入框 `pointer-events: none`；② **焦点守卫**：没按「⌨ 键盘」时谁把焦点塞给它就立刻收回（规矩：只有「⌨ 键盘」按钮能叫出键盘）；③ 点终端时 `wantKeyboard(false)` + 把焦点从输入框收回（blur 不清内容，草稿不丢）；④ 开关那条改成下一拍再 focus | `evidence/键盘与保持/键盘与保持-原始输出.txt`（本地 11 条断言用**真实鼠标点** + 8 条"对话框攒着"断言 + 设备端真触摸点（点终端→焦点 BODY；对照点输入框→焦点落到 cinput）+ 13 个驱动 316 条断言全 true） | 2026-09-21 kbd-A | **已修**（设备端复验通过） |
| R-14 | 真机上点技能行「读技能失败：读不到 … skills\/x\/SKILL.md」；回写内容会变形 | 拿 Android 的 `JSONObject.quote()` 去拼 **python 字符串字面量**：org.json 会把 `/` 转义成 `\/`（JSON 合法、JSON 解码器会还原），python 不认这个转义 → 反斜杠原样留在字符串里。路径被写坏读不到文件；同一处还会把 base64 里的 `/`、带斜杠的模型名写坏 | `Bridge.kt` 新增 `pyStr()`（只转义 python 认的字符），4 处拼字面量全换掉，并注明为什么不能用 `JSONObject.quote()` | `evidence/设备复验/设备复验-原始输出.txt`（第四批：同一路径修后读到 `@@SIZE 343 / @@SHA 56a0baf…`；下载到手机 sha256 与远端一致；回写 358 字节 / 新 sha `78eeed157c10`，宿主侧核对远端文件与备份都对得上） | 2026-09-21 dev-B | **已修**（真设备上复验通过） |
| F1 | 设置页「构建版本」那一行对不上本包：页面 `HP.BUILD=unified-20260921c`、包内 `builtAt=2026-09-22 00:35`，而那个包是 9-22 23:33 出的、还含当晚才改的 `talk.js`（测试者第 1 步报的失败项） | 版本串与打包时间**靠人手写**：出包（`tools/build.sh`）只跑 gradle，没人去改 `build-info.json`；`stamp-build.py` 只把 JSON 盖进 `app.js`，JSON 里的时间戳得人记得更新 —— 忘了就出现「设备上跑的这版还写着上一包」 | 出包时**自动写**：`stamp-build.py` 现按出包时刻生成 `testVersion`（轮次名-日期-时分秒，如同一天出几包也不重名）与 `builtAt`，再盖进 `ui/app.js`；`build.sh` 变成 ①盖章 ②核对 ③gradle ④解开包对账（包内 `build-info.json` / `HP.BUILD` / `ui` 每个文件与源树比哈希）⑤归档 —— **对账不过就不归档**；新增 `stamp-build.py --verify <apk>` 供测试者复用。顺带修掉三处写死 `/home/lwgat/hermes-pocket` 的路径（改成按脚本位置推，换机器/换目录都能用） | `evidence/测试轮-20260923/F1-版本串每包更新-20260923.txt`（旧包对账 FAIL 复现 F1 / 新包 PASS / 页面读数 ↔ 包内读数三方对照 / 手改版本串被 `--check` 拦住并逐字节复原）、同目录 `t-pack-原始输出-20260923.json`；构件 `apk/测试版/hermes-pocket-版本串每包更新-20260923.apk` sha256 `75127e45cdb32207fa7db268bc08829b163f9ae8f4408b93106e8a9760eab408`，`HP.BUILD=unified-20260923-004612` | **已修**（本地测试台 7/7 + 包内对账 PASS；**设备上读数等第 3 步复测**） |

### 本轮新增（本人 2026-09-23 口述，六条；登记照旧：现象 / 根因 / 修法 / 证据 / 状态）

> 排期：**先把要求写下来，等经理有空再排**（本人原话「这些功能让经理慢慢安排，不需要操之过急」）。
> 状态一律先记 `待做`；每条开工前先把"根因"补成**已定位**（要能指出是哪一行/哪条路径），再写修法。

| 编号 | 现象（用户原话式） | 根因 | 修法 | 证据 | 状态 |
|---|---|---|---|---|---|
| R-25 | **省电模式优化** | **已定位（2026-09-23）**：心跳：SSH 用 jsch `serverAliveInterval`（`Ssh.kt:24/184/460`，默认 **30s**），省电时 `app.power{keepaliveSave:120}` **只拉长不关**（`app.js:1568-1587`、`Bridge.kt:926`）；Web 侧另有 20s 一次 `rpc("ping")`（`app.js:693`）。每秒重绘/轮询：每秒 tick（`app.js:130/1649`）、每秒流量采样（`app.js:1702`）、2.5s 聊天轮询（`talk.js:150-157`）、每帧 rAF 看门狗（`app.js:238/1647`）、live 小窗 600ms（`ui.js:118`）、发送状态 500ms（`talk.js:1086-1093`，R-31 新加、空闲自停）。**实测（探针用工程自己的入口重开一遍）**：`resumeWork` 起 2×1000ms、`startPing` 1×20000ms、`Talk.startPoll` 1×2500ms；`pauseWork` 清掉 1000/1000/20000 三个 + 取消 rAF。前台空闲 4s：DOM 变更 1 次、rpc 0 次；群聊页 6s：`talk.since`×2。进省电（实测请求 `{save:true,keepalive:30,keepaliveSave:120}`）：tick/流量/ping 全停、rAF 取消、DOM 变更 0.2/s —— **但 2.5s 轮询没停**（省电下 6s 仍 2 次 `talk.since`，30 分钟≈720 次，每次还走 SSH 起 python 调 `talk.py`），这是当前省电最大的漏点。原生侧：唤醒锁只在 `keepAwake && !powerSave` 时持有（`SessionService.kt:122`，省电即 `releaseWake()`）、省电不重连（`Ssh.kt:375`）、下行改落 `spill-<会话id>.log`（上限 1MB，`Ssh.kt:217/292-303`）亮屏按 ①暂存→②取盘→③关省电→④渲染→⑤放行 补渲染（`app.js:1591-1635`） | 方案：① 把轮询纳入 `pauseWork/resumeWork`（省电停或降频，回前台立刻补一次 `tick()`）；② 与平台侧 ③「聊天要实时」直接冲突 ⇒ **必须给设置项**（候选 A：省电降到 30s；候选 B：只在前台+聊天页保持 2.5s，后台停）—— hint 写清牺牲什么，**口径由本人定**（现有 `powerSave`/`powerSaveDelay`/`keepAwake` 三项在 `panels.js:284-290`）；③ 省电那行读数加「本次省电期间 轮询 N 次 · 补渲染 M 字节」；④ 心跳/唤醒锁/spill 策略保持不动（实测是对的） | `evidence/R25-定位-20260923/R-25-定位-原始输出.txt`（心跳清单 + 定时器清单 + 前台/省电实测 + 判据 6 条 + 方案，sha256 `00df05612470…`）、探针 `source/hermes-pocket/tools/ui-harness/d-r25-power.mjs` | **已定位**（真机耗电量法与侧证写在判据⑤，未量：不接主机、无设备；未验 ≠ 通过） |
| R-26 | **自动本地存储优化** | **已定位（2026-09-23）**：本地分三处互不相干 —— A) WebView `localStorage`：`hp.<设置键>`（约 25 项，`app.js:167/178`、`panels.js:249-280`）、`HP_SWITCH_CFG`（`talk.js:97/100`）、`HP_TALK_CACHE.<k>`（`talk.js:103-104`；写点 `109/154/158/938`；键 = roles/asks/sessions/deliveries/thread.<角色>/draft.<角色>）；B) 原生 `Store`（`filesDir/pocket/`：hosts/keys/known_hosts/**prefs.json**，`Store.kt:44/132/175/254`），**每次写整份重写**（`Store.kt:35-38`、prefs 每键重写 `254-258`）；C) 内存两份：下行环形缓冲 512KB（`Ssh.kt:110-124`）、终端回滚缓冲默认 **5000 行**（`app.js:68/2192`、`panels.js:256` 可到 100000）。**什么时候落盘**：每次 cache 取回来就立即整份写（`talk.js:109`，无节流/无上限/无淘汰）；草稿**每敲一个字写一次**（`talk.js:938`）；设置走 `setPref`→原生，失败才兜底 localStorage（`app.js:167`）；流量区间 30s 节流（`app.js:1745/1755`）；省电时下行输出落 `filesDir/spill-<会话id>.log`（上限 1MB，`Ssh.kt:217/292-303`，取走即删；**旧文件无清理**）。**多大（实测）**：进单聊页一次 3 次写（roles 1,053B + asks 2B + thread **22,533B**）、之后每次重画 1 次 22,533B；平台真实回包 thread(owner.me) **80,994B**；5000 行 × 50 列回滚缓冲堆 **+3.6MB**（756B/行，CDP 量法）；配额实测 ≈**4.99MB**（5,229,060 字符）。**满了会怎样**：原样 `setItem` 抛 `QuotaExceededError`；应用 cache 写**静默吞掉**（`talk.js:104` catch 空）；`app.js:167` 兜底写**异常冒出 setPref** | 方案：① 加「本地占用」读数（界面 localStorage 各键字节 + 原生 store/spill 清单，现无此读数）；② 缓存分层与上限（thread.<角色> 只留最近 N 条 + 字节上限）；③ 写收敛到一个入口 `HP.Cache.set(k,v,{throttle,maxBytes,maxItems})` 内部合并/淘汰；④ 满配额加护栏（提示 + 降级不落盘；`app.js:167` 加 try/catch）；⑤ 原生 spill 加清理；⑥ 设置项 `storageReport`（默认关）、`cacheMaxItems`，省电/省流量档取小值 —— 默认值待本人拍 | `evidence/R26-定位-20260923/R-26-定位-原始输出.txt`（三处清单 + 实测数字 + 原始 JSON + 7 条判据 + 建议，sha256 `ccf022197fb8…`）、夹具 `evidence/R26-定位-20260923/fixtures/*.json`、探针 `source/hermes-pocket/tools/ui-harness/d-r26-storage.mjs` | **已定位**（真机配额/spill 实况由 tester 在设备上量；未验 ≠ 通过） |
| R-27 | **网络统计不全** | **已定位（2026-09-23）**：现有「网络」栏只有 5 项 —— 测试目标（`panels.js:883-886`/`net.js:26-48`，pref `netTarget`）、延迟与丢包（`panels.js:887-890`→`net.js:120-128`→`Bridge.kt:261-274`，远端 `ping -c N -W 2 -i 0.3`，解析 `net.js:57-79`）、端口连通（`panels.js:891-893`→`net.js:130-138`→`Bridge.kt:288-291`＋`TCP_PY`，`@@OK/@@MS/@@ERR`）、原始输出小窗（`panels.js:897-900/967+`）、流量（每秒采样 `app.js:1729-1743`、区间桶/柱饼 `net.js:148-236`、30s 节流落盘 `app.js:1750-1760`、原生 socket 计数 `Ssh.kt:247`→`app.js:335`）。**采集单价实测**：起一次 python3 **14ms**；`net.ping 5 包` **1280ms**（20 包 6080ms）；`net.tcp` **32ms**；一次 SSH 往返（本机↔本机）**374ms** ⇒ 一次 ping(5) ≈ **1.7s**、一次 tcp ≈ **0.4s**（对照 R-25：2.5s 轮询 30 分钟 = 720 次） | 缺项与代价（详见证据）：A 丢包/延迟历史＝**零新增采集**（复用每次读数＋环形落盘）；B 按天/按会话上下行＝**零新增采集**（现有区间桶本地聚合，KB/天）；C 连接次数/失败率/断开原因＝**零网络**（原生 state 事件 `Ssh.kt:165/167/196/221/224/344/381/388`＋重连循环 `372-400`）；D 端到端 RTT 历史＝**已有采集只缺留存**（`app.js:698-702` 的 20s 心跳已经量了 `lastRtt`）；E **定时自动探测**＝真贵：每次 ≈1.7s（SSH 往返＋远端 ping），5 分钟一次 ≈288 次/天 ⇒ 与省电/省流量冲突，**要给设置项**（开关＋间隔，省电档默认不跑）；F socket 级重传/丢包：jsch 取不到 —— **不做**。给本人挑的清单：候选 A（零新增流量：把已在采的三样留成历史＋按时/天聚合）／候选 B（在 A 上加定时探测，默认关） | `evidence/R27-定位-20260923/R-27-定位-原始输出.txt`（现有逐项 + 实测单价 + 缺项代价表 + A/B 清单 + 判据 6 条，sha256 `b77d82d92798…`） | **已定位**（设备↔NAS 的真实往返与候选 B 的耗电/流量未量：不接主机、无设备；未验 ≠ 通过） |
| R-28 | **聊天历史优化** | **已定位（2026-09-23）**：① 频道/群聊：`HP.Talk.msgs` 是**全量内存数组**（`talk.js:122`，无上限/无淘汰/不落盘）；首屏 `rpc('talk.since',{id:0})`（`talk.js:264`）一次**最多 200 条**且平台 `ORDER BY id` 升序（`talk.py:1033-1040`）⇒ 拿到的是**最老**的 200 条，之后靠 2.5s `tick()` 增量续拉（`talk.js:246-253`）；渲染只画最后 80 条（`talk.js:656`）。② 单聊：`thread(role, limit=100)`（`talk.py:1255-1277`）＝**最早 100 条 v_msg ＋ 全部 reply（不限量）**，非升序。③ 会话列表（`talk.js:705-773`）只是会话清单，不是消息历史。④ **搜索/清空：界面零入口**（平台侧有现成 `talk.py search` 未接）。**实测**（本地台喂平台真实回包 200+74 条）：首屏 200 条（id 285..927）而库里最新 1031 ⇒ 打开时最后 74 条不在、要等下一轮 2.5s（库大＝轮数多）；内存 274 条、**DOM 只 80 条**（最老 882 vs 内存最老 285，滚到顶也够不着）且无「加载更早」入口；搜索/清空控件各 0 个；274 条 ≈ **463.5KB** 常驻内存（1732B/条，CDP 堆差）。平台单价：`since --id 0` 0.07s/102,204B、`--id 927` 0.06s/68,419B、`thread --lines 100` 0.06s/134 条（＋SSH 往返 0.37s ≈ 0.45s/页） | 方案：① 平台 `since` 加「取最新」路径（`ORDER BY id DESC LIMIT n` 再反序）＋前端按 id 排序 —— 流量不变，解决"打开看不到最新"；② 翻旧＝按需一页（≤200 条，≈0.45s，用户点了才拉，不轮询）；③ 搜索分层：前端搜已加载（零代价）＋「搜全库」调现成 `talk.py search`；④ 按角色/按天筛选＝零新增采集（在已拉到的 msgs 上过滤）；⑤ 清空只清本地、入口与上限归 R-26；⑥ 后台预取更早历史＝常驻开销，要设置项（开关＋每页条数，省电档默认不跑）| **与 R-26 重叠**：本地留多少条/多大/何时清/占用读数全是 R-26 的活，R-28 只做界面入口＋调用它的清理入口，不另建上限淘汰逻辑；**与 R-30 重叠**：离线翻历史/本地搜索取决于 R-30 的口径（消息落本地库 vs 文案本地化），R-28 第 2 步只做在线部分，等 R-30 定了再排，别造第二份本地消息库 | `evidence/R28-定位-20260923/R-28-定位-原始输出.txt`（现状逐项带行号＋实测＋缺项代价＋重叠点名＋判据 6 条，sha256 `44233e69ed66…`）、同目录 `batch0.json`/`batch1.json`（平台真实回包夹具）、`探针-原始输出.json`、探针 `source/hermes-pocket/tools/ui-harness/d-r28-history.mjs` | **已定位**（设备真触摸与"大库要几轮"未验：不接主机、无设备；未验 ≠ 通过） |
| R-29 | **群聊不自动到最下面** | **已定位（2026-09-23）**：钉底只做在**单聊**一条路上（`talk.js:884` `box.scrollTop = box.scrollHeight`，容器 `#tk-chat`），**群聊那条路一处都没有**：① 新消息到达走 `tick()`（`talk.js:190-196`，2.5s 轮询）→ `paintStream()`（`talk.js:510-530`）把 `#tk-stream` 整块重建后不设 `scrollTop`；② 切栏目进群聊走 `onShow`（`talk.js:128`）→ `render()`（`200-205`）→ `paintGroup()`（`367-377`）→ 同一个 `paintStream()`，同样不钉底；③ 全文没有「在不在底部 / 用户有没有上翻」的状态（grep `scrollTop` 只命中 `884` 一处）。本地测试台实测（跑真 `paintGroup`/`paintStream`、读真 DOM）：进群聊 `scrollTop=0`、离底部 3386px，头顶是最老的第 1 条；新消息重画后仍 `scrollTop=0`、最后一条不在可视区；单聊对照离底部 0（钉底） | 方案（钉底收成一个入口）：加 `pinBottom(box,{force})` —— 重画前先判「在不在底部」（`scrollHeight - scrollTop - clientHeight <= 24`），只在底部或 `force`（切栏目/首渲）时置 `scrollTop = scrollHeight`；三处调用点（`paintStream` 新消息、`paintGroup` 切栏目、`paintChannel` 频道重建）接同一入口，单聊 `884` 也改走它；`paintStream` 不再整块 `textContent=''`，改成按 `m.id` 只追加新行（上翻时位置天然不跳）；上翻后出现「⇣ 回到底部（新 N 条）」入口，点它到底并清计数。**必须保住**现在「上翻不被抢回」的行为（实测上翻到中间后重画位置没被动）。出包归 renderer：`bash tools/build.sh --name "R29-群聊钉底" --feature "…" --feature-id "R-29"`（本轮无新增依赖，纯 `assets/ui/talk.js`） | `evidence/R29-定位-20260923/R-29-定位-原始输出.txt`（文件与行 + 探针原始 JSON + 7 条判据，sha256 `31779c08ad280734…`）、探针 `source/hermes-pocket/tools/ui-harness/d-r29-scroll.mjs` | **已定位**；**R29-4「满 80 裁旧后钉底失效」已修（pipeline.author，2026-09-23）**：真因是 pinBottom 拿「裁旧后」的高度与被夹过的 scrollTop 判底（差 97px > 24）；改法＝`paintStream` 进门先取 `_pinned` 作为判底依据、裁旧补偿按 `scrollHeight` 真实缩减（含 margin）且只在上翻时补偿、两个滚动容器加 `overflow-anchor:none`。本地台读数：追加 1/3/5 条 **97/655/1585px → 0/0/0px**、pinned true、入口不再误弹；未满 80 仍 0px；上翻锚点位移 **267px → 0px**、入口计数 3；老节点仍是同一批 DOM 对象。证据 `evidence/R29-修复-20260923/`（修前/修后原始 JSON + 对照表，sha256 `307639401a68…`）、探针 `tools/ui-harness/d-r29-4-trim.mjs`。设备真触摸复测归 tester（未验 ≠ 通过） |
| R-32 | **单聊页每 2.5s 抛 `pullRoleOutput is not a function`**（`talk.js` 4 处调用、0 处定义） | **已定位 + 有改法（2026-09-24，判定＝删调用不补定义）**：① 4 处调用点与时机 —— `talk.js:173`（**每 2.5s 轮询回调里**，紧跟 `tick().catch()` 之后）、`talk.js:1024/1025`（发完消息后 +2.5s/+6s 两个 setTimeout）、`talk.js:1065`（每次 `paintChat` 末尾）；**异常没有任何 try/catch 吞**，直接打 pageerror（173 那处的 `tick()` 自带 catch，所以未连累刷新）。② 实测（三段各 30s）：现状 **12 条/30s、间隔 2500ms**（全是 `this.pullRoleOutput is not a function`，同段 `talk.since` 12 次）→ 删调用后 **0 条**（`talk.since` 仍 12 次，刷新未受影响）→ 补定义后 0 条错但 30s 被调 **12 次**（真实现内部是 `rpc("talk.capture")` ⇒ 每 2.5s 一次 SSH+python，与 R-25 省电直接冲突）。③ 判定理由：他的回话今天已由两条更好的路覆盖 —— `talk.thread`（走 reply 的话，本来就是单聊气泡的数据源）+ 单聊页「**终端**」开关（`talk.capture` 手动看，talk.js:931）；老实现 `lastAnswer()` 是"从整屏重画的终端里按框线猜他的回答"的脆弱启发式（`5125614` 引入、`f8f031d` 整段重写时删了定义、4 处调用没跟着删）。④ 改法：删 `talk.js:173` / `1024-1025` / `1065`；**顺带第二处死代码** `talk.js:1047` `const live = ((this.live||{})[r.full_name]||[])` —— `this.live` 是布尔（141/166/187/250/275），永取不到角色键 ⇒ 1046-1052 那段 live 分支是死的（没人给它写过值），建议一并删或给布尔改名。⑤ 牵连面：单聊刷新不受影响、R-26 的 `thread.<角色>` 缓存无关、R-31/R-36 送达状态行无关（走 send() 自己的状态机）。读数与全文见 `evidence/R32-定位-20260924/` | 第 2 步：删 4 处调用（+建议收 1046-1052 死代码）后重出包（归 renderer）；复测板块 ① 单聊（主块，**判据＝连续 30s 内 0 条 pageerror/console.error** + 气泡仍刷新 + 发完仍有终态）+ 跨块 ② 群聊（共用同一个 `this.timer`/`tick()`） | 待做 | **待做**（作者已定位，等放行第 2 步） |
| R-30 | **聊天消息客户端本地化** | **已定位（2026-09-23，口径待本人一句话）**：① 消息存哪 —— 内存 `HP.Talk.msgs` 全量无上限（`talk.js:122`，R-28 实测 274 条≈463.5KB，重启即丢）；本地**只有单聊那一份**：`talk.thread` 缓存（`talk.js:1043`→`HP.Cache.set`，`talk.js:102/121-122`），实测打开过的角色留下 `thread.pipeline.tester` **38,350 字节**；**频道/群聊消息一份都不落盘**（`tick()` 拉回来的只进内存 `talk.js:246-253`）。② 断网重启实测（探针强制桥失败 + reload）：**单聊 34 条气泡仍能从本地画出来**（同时如实带「连不上/读不到」字样），**频道/群聊 0 条只显示「（还没有消息）」** —— 且只有「打开过」的角色才有这一份（被动缓存）。③ 界面文案：**无任何 i18n**（无 i18n/`navigator.language` 命中；`res/values/strings.xml` 只有 3 条 string），中文全是硬编码 —— `assets/ui/*.js` 含中文字面量合计 **1,013 处**（panels 319 · app 317 · talk 201 · registry 40 · tui 36 · chatstate 28 · remote 26 · net 24 · ui 8 · transport 6 · cache 4 · sessions 4） | **两份候选（口径由本人挑，我不拍）**：**A＝消息落本地/离线可看**（频道＋打开过的单聊各留最近 N 条，建议 N=200）：新增流量 **0**（数据本来就要拉）／存储 **+**（按实测单价 单聊 22–81KB·角色、频道 200 条≈100KB；10 角色 ≈0.5–1MB ≈ 配额 4.99MB 的 10–20%）⇒ **必须共用 R-26 的 `HP.Cache` 上限/淘汰**／耗电 **0 新增定时器**（只在已有渲染路径写）。**B＝只做界面文案本地化**：范围 1,013 处 + 词条表 + 语言切换；新增流量 **0**／存储 **0**（词条进包 +几十 KB）／耗电 **0**；代价在改动面与回归——tester 很多判据读界面文字（如「服务端：连不上（还没连接）」），换语言会让判据失效，要么判据改读 testid、要么词条锁死；还要你定「只做中文措辞」还是「一并多语言」 | **分工边界（点名，别造第二份本地消息库）**：R-26＝落盘层 `HP.Cache`（上限/节流/淘汰/占用读数/清理，`cache.js` 即 R-26-2 正在做），R-30-A **只用它**、不新写 setItem 不新开键空间；R-28＝历史入口/搜索/翻旧（在线部分），离线翻旧要用它的入口、不另做一套；现在本地已有 `thread.<角色>` 这一份消息，**A 是把它规范化（改名＋补频道那一份），不是再建一份** | `evidence/R30-定位-20260923/R-30-定位-原始输出.txt`（现状逐项带行号＋断网重启实测＋1,013 处文案统计＋A/B 代价＋判据 6 条，sha256 `37cc169d6d3f…`）、同目录 `thread-pipeline.tester.json`（真单聊记录夹具）、`探针-原始输出.json`、探针 `source/hermes-pocket/tools/ui-harness/d-r30-offline.mjs` | **已定位**（设备真断网复验未做；口径等本人；未验 ≠ 通过） |
| R-31 | **发送消息的迟滞以及反馈**（本人 2026-09-23 口述：两种读法都要覆盖） | **已定位（2026-09-23）**：① **界面侧** —— `talk.js:895-906` `send()` 只 `await rpc('talk.say')`（`899-900`）**不看回执**，紧接着 `talk.js:901` 一句 `toast('已发出')` 就结束；而两端其实都回了回执：原生 `Bridge.kt:754-765` 回 `{to,kind,delivered:bool,raw}`、平台 `talk.py:815-818` 回 `{id,delivered[,error]}` —— **回执没人读**；界面也没有 per-message 状态（`Talk` 上无 ack/state 字段，`HP.App.toast` 默认 1900ms 自动消失）。② **平台侧** —— `talk.py:880-925` `deliver()`：多行固定 `sleep 0.8`(`915`) + `sleep 0.6`(`917`) 再补一次 Enter（`check=False`），单行 `sleep 0.12`(`922`)；广播是**串行**逐角色投（`800-806`）⇒ 每多一个目标约 +1.0~1.4s。`delivery` 表只有 `(msg_id, role, at, ok, note)`（`talk.py:66`），**没有耗时列**、`at` 只到整秒；`talk.py:1280-1290` 的「没回音」判定拿 `ok=1` 当真送达，而 `ok=1` 只等于「tmux 命令没报错」 | 方案：界面加发送状态机三态（发送中 / 已送达 ◯◯ms / 没送达:原因）+ 8s 超时与重试，状态挂在该条消息上（不增常驻按键）；平台 `deliver()` 前后计时、`delivery` 加 `ms` 列、`say()` 把每目标 ms 与结果回给界面，并把 `ok` 分成「命令成功 / 读回确认」；新增设置项 `sendTiming`（默认开，省电/省流量档默认关、只留终态）。出包归 renderer：`bash tools/build.sh --name "R31-发送反馈" --feature "…" --feature-id "R-31"` | `evidence/R31-定位-20260923/R-31-定位-原始输出.txt`（链路 `file:line` + 拦桥探针原始 JSON + 平台台账实测差值表 + 8 条判据，sha256 `6fddf64df7ed1511…`）、探针 `source/hermes-pocket/tools/ui-harness/d-r31-send.mjs` | **已定位**（真点由 tester 在第 3 步做；未验 ≠ 通过） |

## 环境与归档约束（做事时别忘）

- 构建/测试：容器 `notifbridge-emu`（无头模拟器 + adb），依赖走本地镜像；**先停模拟器腾内存再构建**，重活串行。
- 工作副本 / 真源 = `/vol1/1000/airesults/hermes-pocket/source/hermes-pocket/`（git 仓库在 `/vol1/1000/airesults/hermes-pocket/`）。
  **`/home/lwgat/hermes-pocket/` 是 9-21 的旧副本**（连 `ui/talk.js` 都没有），别再从它构建或测界面 —— 界面测试台与 `t-pack.mjs` 原来写死的那条路径已改成按脚本位置推。
- 证据存 `evidence/<主题>/`，脚本存 `tools/验收脚本/`；构件哈希每次更新。
| R-33 | **触控目标小于 44dp**（R26-3 复测的真机坐标读数：看明细/清理 42px、群聊行内键 40px、台账键 32px） | **已定位 + 有改法（2026-09-24）**：① 设置页两键 42dp＝`style.css:263 .btn{min-height:42px}`（panels.js:685-686）② 群聊喊话行 38-40dp＝`talk.css:42 .tk-askin` / `talk.css:46 .tk-act` / `talk.css:3 .tk-chip` 三者同行 stretch（talk.js:440-463）③ 台账键 32dp＝`talk.css:3 .tk-chip{padding:6px 12px;font-size:13px}`（talk.js:473-478）。改法＝4 条 CSS 只加 `min-height:44px`+居中（不动排版方向/宽度）；本地台注入实测：42/42/38/38/38/32 → **44×6（ge44 全 True）**，点台账键下沿真触发（`delivOpen` False→True），间距不变（8px/0px）、无换行无横向溢出。px↔dp：viewport `width=device-width` ⇒ **1 CSS px = 1 dp**（设备 393dp×2.75）。读数与全文见 `evidence/R33-定位-20260924/` | 第 2 步：改这 4 条 CSS + 出包归 renderer；**未拍**：SPEC §10「相邻间距 ≥44dp」是否也要求（现状 8px/0px，要拉开得改排版） | **已验收（2026-09-24 R33-3）**：44dp 全达标（看明细 71×44 / 清理 57×44 / 台账 77×44 / 喊话行三件 44），真机真触摸 5/6 绿 | **已收口**（第 3 步判 done；红项「清理无确认」转 **R-35**） |
| R-35 | **「清理」键没有确认**：点了直接执行（真机实测 `window.confirm` 调用 0 次、无弹窗），只 toast「清掉 0 个键，释放 0.0KB」 | **已定位（2026-09-24 R33-3 复测真机）**：设置页清理键（`panels.js` 清理处理 + `talk.js` 本地缓存清理）直接执行；键有 44dp 且真触发 ✓（这条不是命中区问题，是**破坏性动作没有二次确认**） | 口径（经理已拍）：**必须有确认**，且提示里写清**将清掉几个键/多少 KB**（现成读数就在 toast 里）；N=0 时可只提示不清 | 待派（等 R34 收尾后给 author 定位→renderer 改+出包→tester 复测） |
| R-36 | **「不接主机时广播也显示『已送达 ≈29ms』」有假成功嫌疑**（tester 复测观察，未定性） | **已定位（2026-09-24）：判定＝假成功**。根因两处：① `talk.js:1181` `ok = (bridged === null) ? true : !!bridged`（回执缺 delivered 就当成功）+ `talk.js:1183-1184` 没 ms 就用界面往返毫秒加 ≈；② `Bridge.kt:748-752`（talk.shout）runCatching 吞异常后**无条件 ok**、只填 `{broadcast:true, raw:{}}`，`Bridge.kt:763-764`（talk.say）`delivered = r.isSuccess`（命令没抛错）**不是平台投递结果**。实测：广播+不接主机 →「已送达 ≈3ms」state=sent（复现真机 ≈29ms）；平台 raw 说 `delivered:false` 也照样写「已送达」。真读数只在：广播连主机走 `raw.results`（1/2 已送达 1491ms）、桥 `delivered:false`、桥报真错、8s 超时。改法：A 缺 delivered ⇒ 新增第三态「已发出（未确认）」（talk.js:1176-1186 + 文案/配色）；B talk.shout 别吞异常、补填 delivered；C talk.say 的 delivered 改读平台 raw。读数与全文见 `evidence/R36-定位-20260924/` | 第 2 步：改 `talk.js` + `Bridge.kt` 后重出包（归 renderer）；复测板块 ② 群聊（连着/不接主机两种）+ ① 单聊（三种回包），跨块理由＝同一套发送状态机 | 待做 | **待做**（作者已定位，等放行第 2 步） |
| R-37 | **单聊页点消息头点不开信息窗**（tester R34-3 真机：真触摸后 `#tk-sheet` 不出现） | **已定位 + 有改法（2026-09-24）**：① 复现 —— CDP 真触摸点他人气泡/抬头都**不开窗**；代码同证 `paintChat()`（talk.js:1034-1066）无 click、无 openRoleSheet；群聊在 `paintStream()`（talk.js:676-680）才有。根因：两条路数据形状不同（群聊 `m.from`；单聊 `talk.thread.items` 是 `who`，无 from），paintChat 是后写的独立段、既没挂监听也没设 data-from。② 改法 4 处：A `talk.js:934-937` 抬头 `.tk-title` 挂 click→openRoleSheet（主入口）；B `talk.js:1041-1058` 非本人气泡挂（`data-from=r.full_name`，与群聊同构）；C `talk.js:1060-1064` live 那批同样挂；D 8px 位移守卫（touchstart/move，拖动即放弃）。③ 注入版实测：正向开窗✓✓；三条负向（本人气泡/空白/在气泡上拖 80px）全不开窗、滚动照常（scrollTop 62568→62633）；冲突检查：长按选词（body user-select:none）、点链接（textContent 无 a）、抽屉/滚动手势（不碰 touchmove/preventDefault）均不冲突。读数与全文见 `evidence/R37-定位-20260924/` | 第 2 步：改 `talk.js` + 出包（归 renderer）；复测板块 ① 单聊（主块）+ ② 群聊（同结构回归） | 待做 | **待做**（作者已出改法，等放行第 2 步） |
| R-38 | **底部固定条带压住列表末尾**（tester R34-3：第 5 张卡 home.maid 中心 `elementFromPoint=null`、hit=false；「去群聊 →」落在视口以下 px(12,882,87×44)） | **已定位 + 有改法（2026-09-24）：本地台**（393×778、真 DOM）**复刻了读数、但判定不是遮挡** —— 未滚到底时末卡 y=629..689、中心命中 `TEXTAREA`、重叠 **77px**（复刻 tester）；**滚到底（scrollTop=503）后末卡 y=126..186 命中 `BUTTON.card`、「去群聊 →」y=544..588 命中 `BUTTON.tk-act`、重叠 0px**。固定层清点：`#composer` 612..681（h=69）+ `#keybar` 681..778（h=97）共 166px，**两者都是 `#app` flex 列里的流内元素（position:static、z-index 30），不是 fixed 浮层**；滚动容器＝`.panel-body`（**无 `#tk-roles`**）y=85..612、可滚 503px、`padding-bottom: 24px`（**死值、不含 safe-b**；两层自身都带 `calc(… + var(--safe-b))`）。⇒ tester 那两条是**没滚到底**时的读数（反推 scrollTop 只走了一小部分）。真机无法排除的一条：若「滚到底也不可达」，成因是 `--vh/--vtop` 与可视区失配（app.js:188-230），**需 tester 补四个数**：`.panel-body`.rect.bottom / `#composer`.top / `innerHeight` / `visualViewport.height`（本地对照 612/612/778/778）。改法：① 复测口径＝判可点前必须先滚到底/scrollIntoView（本轮 2/3 轮的红源于此，不出包）；② 要把「末尾到固定层间隙」抬到 §10 ≥44dp ⇒ `style.css:223` 的 `padding-bottom: 24px` → `calc(44px + var(--safe-b))`（一行，全 `.panel-body` 板块共用）；③ 若真机证实 --vh 失配再改 `#app` 高度口径（等读数）。读数与全文见 `evidence/R38-定位-20260924/` | 第 2 步（若走 ②：改 style.css + 出包，归 renderer）；复测板块 ⑧ 频道页（主块，**真机滚到底点末尾卡与「去群聊 →」必须有**）+ 跨块 ① 单聊/② 群聊/③ 设置页（共用 .panel-body） | 待做 | **结案（2026-09-24 R38-1/R38-2）**：**非缺陷** —— 本地台与真机都证明「滚到底后末卡与『去群聊 →』都在可视区、中心命中自己、与 #composer 重叠 0px、真触摸都真触发」；前两轮 hit=false 是**没滚到底**量到的（新口径已写进 SPEC §10）；真机四个数 594/594/778/778 排掉 --vh 失配；末尾间隙 **24px** 要不要抬到 ≥44dp **等本人 A/B**（A 不改／B 改 style.css:223 一行） |
| R-39 | **单聊页末条气泡被 sticky 输入条压住**（tester R32-3/R37-3 真机：`#tk-sayline` y510..570 重叠 56px、点中点命中 `INPUT.tk-askin` ⇒ 末条气泡点不开信息窗，**滚到底也如此**） | **已定位 + 有改法（2026-09-25）**：① sticky 条＝`#tk-sayline`（talk.js:1113-1125，`position:sticky`、`bottom:0`、**高 60px**、不透明背景），粘在滚动容器 `.panel-body` 可视底边上（实测 y=528..588）；**为什么盖得住**：DOM 顺序是 `paintRole`（…→`#tk-chat`→chips→`#tk-sayline`）之后**再 `paintSends(el)`**（render talk.js:373-375）⇒ 粘性条下面还有内容，贴底时必然压住它上面那段（聊天框下沿）。② 重叠实测：**未滚到底**（外层 scrollTop=0、内层已在底）末条 366..577 vs 条上沿 528 ⇒ **重叠 49px**、下沿 `elementFromPoint` 命中 `INPUT.tk-askin#tk-sayin`（复现 tester）；**外层也滚到底**（可滚 101px）⇒ 重叠 0 —— 用户手指在气泡上滑只滚**内层**（`#tk-chat` 可滚 70892px），所以真机"滚到底仍压住"成立。`#tk-chat` padding-bottom 现状 **0**、`.panel-body` 24px。③ 判定＝**不是缺通用底部内边距，而是 sticky 条压在滚动内容上、内层没为这 60px 留白**：实测对照（都在"外层不滚"的真实状态）——**乙**给内层补 `#tk-chat{padding-bottom:calc(68px + var(--safe-b))}` ⇒ 末条下沿 577→**509**、重叠 **0**、中心命中＝**气泡本身** ✔；**甲**给外层 `.panel-body` 补同样内边距 ⇒ 重叠反而 **93px（更糟）** ✘。④ 改法：**talk.js:1040-1048**（`#tk-chat` 内联样式）加一行 `paddingBottom`（或落 `talk.css` 的 `.tk-chat`）；建议顺手把条高做成 CSS 变量（`--sayline-h`）避免字号/安全区失配。「两套 tk-sayin / 重复 id」核实：本机台 `#tk-sayin`=**1**、`.tk-askin`=1、`#tk-sayline`=1 ⇒ **无重复 id**；页面上另有全局 `#composer`（发给 tmux，常驻 69px），"像两套输入框"更可能是它。⑤ 与邻居：R-38（频道页未滚到底＝非缺陷）不冲突（本例滚到底仍复现、是 sticky 覆盖）；**R-42** 让人更多进单聊 ⇒ 更易撞上，且 R-42 ③ 若复用该结构须同样留白 ⇒ 建议写成共用规则"粘性底部条 ⇒ 上方滚动内容留 calc(条高+var(--safe-b))"；⑥讨论 B 案换条也要保留该规则。读数与全文见 `evidence/R39-定位-20260925/` | 第 2 步：改 `talk.js`（+可选 `talk.css`）后重出包（归 renderer）；复测板块 主块 **①聊天页-单聊**（判据＝只滚内层到底后点末条气泡能开信息窗；三读数 滚到底/scrollIntoView/elementFromPoint）+ 跨块 **⑧频道页**（R-42 ③ 若复用） | 待做 | **待做**（作者已定位，等放行第 2 步） |
| R-40 | **会话/角色列表看不出是谁**（本人 2026-09-24：找不到哪个 session 指向自己、也不知道编号怎么看） | **已定位 + 有改法（2026-09-24）**：① 现状实测（真回包 + 真 tmux 原文）——**⑤会话页**（panels.js:821-859）只有 **2 行 tmux 容器**（hermes / roles，0 条"人"、0 编号）；**⑧频道页「上次聊过」**（talk.js:706-780）只有一组「历史（14）」、14 行全是 `X · 历史和话（hermes session）`、0 编号（**顺带缺陷③**：talk.js:710 读 `r.roles` 而 roles-json 只有 `scenes` ⇒ 「在线/没在线」分组永远空、known 恒空 ⇒ 同一个人可能列两条）；**角色卡/信息窗**（talk.js:506-535 / 809-830）副行与「会话」用的是**合成名** `sess()`（talk.js:137 `role-*`，只跟一条已废记录撞上），真值应是 `#6 · owner-me`、`#10 · pipeline-author`、`#4 · 本人-女仆通道`。② 编号取 `talk.sessions[].id`（App 早就在拿，talk.js:709）——role→session 按 `s.role` 匹配、多条取 `alive` 者、没有就不显示号码（**前端不造号**），会话页与信息窗共用同一映射。③ 去重**按 tmux**（`会话:窗口`）不按 role（本人定的：QQ 通道是本体、App 入口是第二入口，按 role 去重会把这两条合掉；同 tmux 只留 alive 那条；同 role 多条都显示、废的标「已废」；solo 归「临时对话」；容器 roles/hermes 不成行）。④ 命名统一：窗口名直接用 tmux 冒号后半段；QQ 通道显示「本人-女仆通道」（⚠ 需求文档写"在 hermes 会话里"，真值是 `qqbot:3FDE…`，界面按本人叫法显示）。⑤ 改法落点：talk.js:137 换成 `sessInfo(full)`、519 副行、824「会话」行、panels.js:821-859 会话页换主数据源为 `talk.sessions` + 顺带修 710 的 `r.roles`→`scenes`。读数与全文见 `evidence/R40-定位-20260924/` | 第 2 步：改 `talk.js` + `panels.js` 后重出包（归 renderer）；复测板块 主块 **⑤会话页** + **⑧频道页**（角色卡/信息窗/历史），跨块 **①单聊**（信息窗同源）；判据＝每条 `#id`+身份、与信息窗同号同人、同窗口不出现两条、其它板块不挤坏 | 待做 | **待做**（作者已定位，等放行第 2 步） |
| R-41 | **App 频道里按回车不能确认/发送**（本人 2026-09-24 当场：与某人对话时「他无法输入 enter 键表示确认」） | **已定位 + 有改法（2026-09-24）**：① 接线现状实测——群聊 `#tk-shoutin`（talk.js:492-509，只挂 keydown→Enter→gfire）、单聊 `#tk-sayin`（talk.js:1058-1083，input 存草稿 + keydown→Enter→fire）**两处 enterkeyhint 都缺**（全仓 0 处），**授权回复框**（talk.js:598-620 `.tk-askin`）**连 Enter 都没挂（漏挂）**；投递侧复核：`Bridge.kt:772 talk.say` → `talk.py:1039 deliver()`（paste-buffer + Enter + 双回车兜底）**没问题**。② 本地台复现（真键盘/CDP 输入法形状事件）：**真 Enter 能发**（群聊 shout=1、单聊 say=1），事件序＝keydown(13)→keypress(13)→beforeinput(insertLineBreak)→keyup(13)；**"输入法提交（insertText，无 keydown）+ 只补 keyup" 与 "只派 beforeinput" 都发不出去（=0）** ⇒ 机制吻合；但**真机各输入法给哪些事件本地台给不出答案**，不给"已复现"结论。③ 候选接线实测（页面注入）：keyup 兜底→1、beforeinput 兜底→1、**一次按键三条路都触发只发 1 次**、连按两次发 2 次；**⚠ 抓到 Shift+Enter 被 beforeinput 兜底误发 1 次**（单行 input 无换行）。④ 改法：① `enterkeyhint="send"` 加 talk.js:492-494 / 1058-1060（+授权回复框 598-600）② keyup + keyCode===13 兜底 ③ beforeinput/input 判 `insertLineBreak|insertParagraph` 兜底（唯一能救"完全没按键事件"的路）④ 换行共存两案（**待本人拍**）：A 保持单行 input、三条兜底加 `!shiftKey`（Shift+Enter 不发送也不换行）；B 换 `<textarea rows=1>`+自动长高（先例 app.js:1892-1893），Enter 发送 / Shift+Enter 真换行，代价＝改动面大。⑤ 防重复：每框一个 `sentAt` 时间窗（建议 400ms）＋发完清空 value（输入法那条路 value 非空，所以时间窗不能省）；仓库已有"两边都接管=一次回车发两遍（本地实测抓到过）"的先例注释 app.js:1901-1903。读数与全文见 `evidence/R41-定位-20260925/` | 第 2 步：改 `talk.js` 后重出包（归 renderer）；复测板块 主块 **②群聊** + **①单聊**（判据＝真机按输入法回车能发出：出现自己气泡 + 对方会话条数 +1；换行按 A/B 口径；同一次回车不出两条），跨块 **⑧频道页**（授权框也补了 Enter）；建议真机至少两种输入法并记下事件序列 | 待做 | **待做**（作者已定位，等放行第 2 步） |
| R-42 | **「频道」页点开不能聊天**（本人 2026-09-24 当场：「我点频道根本不能聊天」） | **已定位 + 有改法（2026-09-25）**：**不是"进不去"，是"频道页本身没有能说话的地方"**。实测（CDP 真触摸）：**A「频道（N）」折叠区**（talk.js:471-495）= 点了**只展开/折叠**一行**只读**的「qqbot → home.maid」（行高 36px，**再点行毫无反应**）—— 字面最像"频道"，本人多半点的就是它；**B 频道页没有消息流**：`#tk-stream` 是**群聊页**建的（talk.js:529-544 paintGroup），频道页实测 false、对照组群聊页 true；**C 角色卡**（talk.js:613-638）点一下 = **开信息窗**（不直接进单聊），要在信息窗里再点「跟他对话」（talk.js:973）才进单聊 ⇒ **从频道页到"能打字"= 2 次点击**；**频道页里 0 个聊天输入控件**（`#tab-talk` 下只有「＋新角色」表单的 4 个 input）。与 R-41 边界：R-41 是"输入框里回车不发送"（已修，三处转绿），本条是"从频道页到有输入框这条路"，**无重叠**。改法（代价从小到大）：① **角色卡单击直接进单聊**（talk.js:637 `openRoleSheet`→`openRole`），信息窗入口挪到右侧箭头 `.tk-caret`（talk.js:620-624，按 R-33 给 ≥44dp）或改长按 ⇒ 一步进单聊；② 给 `.tk-chrow`（talk.js:487-495）挂 `click → openRole(该频道第一接收者)` 并写清「点这里跟他说话」，不做就加「（只读）」字样；③（与 ⑥讨论 B 案同一件事）频道页加"跟谁说"输入条，复用 talk.js:192-215 `bindEnterSend()` + `talk.say`，约 30-60 行，等 ⑥讨论定口径。冲突检查：R-37 单聊页入口不冲突；**R-40 显示不受影响但"点卡片看信息"的入口变了 → 要写进 R-40 复测口径**；R-33 新热区要 ≥44dp。读数与全文见 `evidence/R42-定位-20260925/` | 第 2 步：改 `talk.js` 后重出包（归 renderer）；复测板块 主块 **⑧频道页**（判据＝从频道页**一步**进到能给某人发消息、发出后 App 出自己气泡）+ 跨块 **①单聊**（信息窗入口变化后"点抬头开信息窗"仍好使） | 待做 | **待做**（作者已定位，等放行第 2 步） |
| R-43 | **发送后状态总显示「已发出（未确认）」**（本人 2026-09-24 当场："说发送成功……然后说未确认的什么状态"） | **已定位 + 有改法（2026-09-25，判定＝根因在平台侧）**：**① 平台没给机器可读回执** —— `talk.py say` 的 CLI 打的是**人话**（`print("#%d 已记入并投给 %s（投递%s）")`，talk.py:2459-2466），而同仓给 App 用的子命令都打 `json.dumps`（2258/2282/2286…）⇒ 桥 `JSONObject(out)` **必然抛异常** ⇒ 走兜底 `{raw: 人话, cmd:…}`（Bridge.kt:120-126）⇒ `raw.has("delivered")=false` ⇒ `delivered=null`（Bridge.kt:790-794）⇒ App 按 R-36 中性态显示「已发出（未确认）」（talk.js:1327-1328、1345-1347）。**② 即便解成 JSON，平台那个 `delivered` 也只是"deliver() 没抛错"**（talk.py:896-900）＝R-36 判死的假成功语义。**③ App 侧是正确的降级**：四种回执形状喂桥实测 —— A 现状真形状→「已发出（未确认）」（复现本人现象）；**B 只要平台给 `{delivered:true, ms:1491}` → App 立刻显示「已送达 1491ms」（App 一行都不用改）**；C `delivered:false`→「没送达：没有会话」；D 只给 `confirmed`（乙案）→ 仍是「未确认」⇒ 乙案必须改 App 才生效。真回执平台**已经有**（O-1）：`last_deliver_ms/attempts/confirmed`（talk.py:1091/1105/1074-1102）+ `delivery` 表两列（182-183、717-720），**只是 `say()` 从来没带出去**。改法：**甲（推荐，我投甲）** `say()` 两分支带上 `ms/attempts/confirmed` 且 **`delivered` 判据改成 `confirmed==1`**（`None`⇒中性），CLI 改打 JSON ⇒ **只动 talk.py 一个文件、约 10-15 行、不需要重出 APK**；判据＝App 显示必须与台账那一行对得上，复算 `sqlite3 talk.db "select msg_id,role,ok,ms,attempts,confirmed,note from delivery order by at desc limit 5"`。**乙**（App 认 `confirmed`）＝动 `talk.js`+`Bridge.kt` 两个文件、**必须重出包并真机复装**，且治不了根。甲只把"真确证"从中性态升级成「已送达+真 ms」，R-36 的三态一个不删。读数与全文见 `evidence/R43-定位-20260925/` | 第 2 步：**甲归 owner.me（平台侧 `talk.py`，改完需重启 relay 生效）**；若定乙则归 renderer（改 talk.js+Bridge.kt 出包）；复测板块 主块 ①单聊 + ②群聊（广播分支同样带真回执）+ 台账复算一条 | 待做 | **已修（甲案·平台侧，2026-09-25）**：`say()` 带真回执（`delivered` 判据＝O-1 的 `confirmed`；`0`/读不到→`null` 中性）+ CLI 改打 `json.dumps`；App 端**一行未改**；R43-3 复测 **5 项全绿**（真形状→「已送达 <ms>」；三态齐；广播 partial/全确证；回归全在；一次回车 1 次 RPC）。**ms 口径（经理拍）**：App 显示回执里的 ms（`say` 那次投递实测），台账那行可能由中转站补记 ⇒ **允许 ≤200ms 差、`confirmed` 必须一致**；真机+真平台端到端仍记**未验**（需一次主机连接） |
| R-44 | **底部那条给 tmux 会话用的输入框，在「频道」里跟人对话时也一直留着**（本人 2026-09-24 口述：「我在频道里沟通的时候，那个输入框一直保留在那里，就不太合理。如果你保留在那里，那按道理说……我可以使用它，把这段话发送给频道里的某个角色」） | **待定位（留档，未开工）**：那片区域「输入条该不该在、在的时候收件人是谁」不清楚；与 **R-41**（频道里回车不能确认/发送）、**R-42**（频道页点开不能聊天）**同一条线**，要放一起看 | **候选两档（等本人拍，别自己定）**：**A**＝不该出现的场景（tmux 会话页以外）就不保留输入条；**B**＝保留但必须真能用（能指定发给频道里的某个角色） | **留档（下次改）**；口径不清处**待跟本人确认再动手**。**本人 2026-09-24 倾向＝A（非本场景不保留输入条），但不拍板** ——「明天让他们自己去讨论哪个更加合理」⇒ 按 ⑥ 流程**明天**由 author/renderer/tester 各出理由+证据（大段走文件）→ 经理写决定书 `roles-chat/docs/决定-2026-09-25-输入条口径.md` → 三人一人一票、经理只记票署名 → 事后通知本人（可推翻）。**讨论一次说清同一片输入区**：把 R-41（回车不能确认/发送）、R-42（频道页点开不能聊天）一起串进来 |
| R-45 | **Shift+Enter 会误发**（tester R41-5：`input keycombination 59 66` 模拟实体键盘 —— `keydown Enter(shiftKey=true)` 主路已拦，但 `keyup` 兜底那条路用 `e.shiftKey` 覆盖了判定：Shift 先松、Enter 后松 ⇒ 当次被判成「没按 Shift」→ 真的发出去了，body 落地、输入框清空） | **已定位（真机/事件序列）**：根因＝`keyup` 兜底里 `shiftDown = e.shiftKey` 覆盖 keydown 的判定；修法方向＝Shift 状态**只由 Shift 键自己的 keydown/keyup 维护**（或用 `e.getModifierState('Shift')`），Enter 的 keyup 不得改写它；`beforeinput` 兜底同样要带上「这次是谁触发的」 | 判据：实体键盘 Shift+Enter → **0 条且文字留着**；普通 Enter → 照旧 1 条（不许为修它把主路弄坏） | **已建三步链 `hermes-pocket-R45`**（待放行，按本人清单排在 R42/R43/R39 之后；只影响有实体键盘的场景，优先级低） |
